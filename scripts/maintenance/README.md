# Maintenance scripts

Maintenance scripts must be portable and operate relative to the repository root. They must not encode a developer-specific `E:\` or `C:\` path and must not touch `CRESTU_GPT6PRO_FIXED/GPT6PRO_ONE_SHOT/NextStep`.
