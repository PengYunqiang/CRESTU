diff --git a/Source Code/1.Input/read_config.m b/Source Code/1.Input/read_config.m
index 4b831b7..e760cf9 100644
--- a/Source Code/1.Input/read_config.m
+++ b/Source Code/1.Input/read_config.m
@@ -235,6 +235,7 @@ function controls = parse_phase2_2_controls(sections, legacyFs)
         'inputMode', 'legacy-absent', ...
         'fsRadialCounts', [legacyFs.nr_near, legacyFs.nr_sponge], ...
         'bottomRadialCounts', [legacyFs.nr_near, legacyFs.nr_sponge], ...
+        'bottomCoreRadialLayerCount', 0, ...
         'outerVerticalPanelCount', legacyFs.nz_farfield, ...
         'outerThetaPanelCount', 0, ...
         'freeSurfaceOuterThetaPanelCount', 0, ...
@@ -246,6 +247,7 @@ function controls = parse_phase2_2_controls(sections, legacyFs)
         'spongeMuPolicy', 'legacy-floor', ...
         'spongeMu0', legacyFs.mu0, ...
         'spongeAutoTuneEnabled', true, ...
+        'polarTopologyMode', 'LEGACY', ...
         'rawDiagnosticsEnabled', false, ...
         'persistFullSolutions', false);
     if ~isfield(sections, 'PARA13')
@@ -269,6 +271,9 @@ function controls = parse_phase2_2_controls(sections, legacyFs)
                 controls.fsRadialCounts = parse_positive_integer_pair(tokens, key);
             case 'BOTTOM_RADIAL_COUNTS'
                 controls.bottomRadialCounts = parse_positive_integer_pair(tokens, key);
+            case 'BOTTOM_CORE_RADIAL_LAYERS'
+                controls.bottomCoreRadialLayerCount = ...
+                    parse_named_positive_integer(tokens, key);
             case 'OUTER_VERTICAL_COUNT'
                 controls.outerVerticalPanelCount = parse_named_positive_integer(tokens, key);
             case 'OUTER_THETA_COUNT'
@@ -299,6 +304,16 @@ function controls = parse_phase2_2_controls(sections, legacyFs)
             case 'SPONGE_AUTO_TUNE'
                 controls.spongeAutoTuneEnabled = parse_named_flag(tokens, key);
                 autoTuneWasSpecified = true;
+            case 'POLAR_TOPOLOGY_MODE'
+                assert(numel(tokens) == 2, ...
+                    'CRESTU:PolarTopologyModeSyntax', ...
+                    '%s requires LEGACY or QUALITY_CONTROLLED_V2.', key);
+                mode = upper(tokens{2});
+                assert(any(strcmp(mode, ...
+                    {'LEGACY', 'QUALITY_CONTROLLED_V2'})), ...
+                    'CRESTU:PolarTopologyModeValue', ...
+                    'POLAR_TOPOLOGY_MODE must be LEGACY or QUALITY_CONTROLLED_V2.');
+                controls.polarTopologyMode = mode;
             case 'RAW_DIAGNOSTICS'
                 controls.rawDiagnosticsEnabled = parse_named_flag(tokens, key);
             case 'PERSIST_FULL_PHI'
diff --git a/Source Code/2.Mesh/apply_rankine_outer_truncation.m b/Source Code/2.Mesh/apply_rankine_outer_truncation.m
index 4269064..40f4424 100644
--- a/Source Code/2.Mesh/apply_rankine_outer_truncation.m
+++ b/Source Code/2.Mesh/apply_rankine_outer_truncation.m
@@ -25,6 +25,8 @@ function [freeSurface, bottom, outerBoundary, audit] = ...
     thetaControls = resolve_theta_controls(cfg);
     thetaResolutionRequested = any([thetaControls.common, ...
         thetaControls.top, thetaControls.bottom] > 0);
+    qualityControlledTransition = ...
+        get_polar_topology_mode(cfg) == "QUALITY_CONTROLLED_V2";
     policy = policy_specification();

     if (~specification.enabled || ~extensionRequested) && ...
@@ -69,11 +71,12 @@ function [freeSurface, bottom, outerBoundary, audit] = ...
     [freeSurface, topRing, topInfo] = prepare_horizontal_surface( ...
         freeSurface, desiredTopRadius, legacyRadius, commonThetaCount, ...
         startAngle, 0.0, topRadialTarget, topThetaTarget, cfg, 2, ...
-        thetaResolutionRequested);
+        thetaResolutionRequested, qualityControlledTransition);
     [bottom, bottomRing, bottomInfo] = prepare_horizontal_surface( ...
         bottom, desiredBottomRadius, legacyRadius, commonThetaCount, ...
         startAngle, -cfg.water_depth, bottomRadialTarget, ...
-        bottomThetaTarget, cfg, 4, thetaResolutionRequested);
+        bottomThetaTarget, cfg, 4, thetaResolutionRequested, ...
+        qualityControlledTransition);
     topInfo.endpointThetaNodeCount = topThetaCount;
     topInfo.wallRingNodeCount = size(topRing, 1);
     topInfo.endpointToWallCountTransition = topThetaCount ~= commonThetaCount;
@@ -115,6 +118,7 @@ function [freeSurface, bottom, outerBoundary, audit] = ...
         'A derived frustum spacing is more than 5%% coarser than its target.');

     audit = struct('schemaVersion', 2, 'policy', policy, ...
+        'polarTopologyMode', char(get_polar_topology_mode(cfg)), ...
         'enabled', true, 'inputMode', specification.inputMode, ...
         'geometryModified', true, ...
         'independentTopQ', specification.topRadiusPerWavelength, ...
@@ -488,11 +492,19 @@ end

 function [mesh, outerRing, info] = prepare_horizontal_surface(mesh, ...
         targetRadius, legacyRadius, commonThetaCount, startAngle, zValue, ...
-        radialTarget, thetaTarget, cfg, panelType, gradualThetaTransition)
+        radialTarget, thetaTarget, cfg, panelType, gradualThetaTransition, ...
+        qualityControlledTransition)
     legacyRing = extract_outer_ring(mesh, zValue, legacyRadius);
     legacyCount = size(legacyRing, 1);
     tolerance = 64 * eps(max([targetRadius, legacyRadius, 1]));
     extension = targetRadius - legacyRadius;
+    if qualityControlledTransition
+        [mesh, outerRing, info] = ...
+            prepare_quality_controlled_one_to_two_surface(mesh, ...
+            targetRadius, legacyRadius, commonThetaCount, startAngle, ...
+            zValue, radialTarget, thetaTarget, cfg, panelType);
+        return
+    end
     if extension <= tolerance && commonThetaCount == legacyCount
         outerRing = legacyRing;
         info = surface_info(0, 0, 0, legacyCount, legacyCount, ...
@@ -568,6 +580,134 @@ function [mesh, outerRing, info] = prepare_horizontal_surface(mesh, ...
         'An explicit horizontal-annulus transition adds more than 4 nodes.');
 end

+function [mesh, outerRing, info] = ...
+        prepare_quality_controlled_one_to_two_surface(mesh, targetRadius, ...
+        legacyRadius, commonThetaCount, startAngle, zValue, radialTarget, ...
+        thetaTarget, cfg, panelType)
+% PREPARE_QUALITY_CONTROLLED_ONE_TO_TWO_SURFACE Use a fixed 1:2 template.
+
+    legacyRing = extract_outer_ring(mesh, zValue, legacyRadius);
+    legacyCount = size(legacyRing, 1);
+    assert(commonThetaCount == 2 * legacyCount, ...
+        'CRESTU:QualityPolarOneToTwoCount', ...
+        'QUALITY_CONTROLLED_V2 requires final theta count = 2*legacy count.');
+    extension = targetRadius - legacyRadius;
+    transitionStep = min(0.4 * max_ring_edge(legacyRing), radialTarget);
+    tolerance = 64 * eps(max([targetRadius, legacyRadius, 1]));
+    assert(extension > transitionStep + tolerance, ...
+        'CRESTU:QualityPolarOneToTwoExtent', ...
+        'Outer extension is too short for the predeclared 1:2 connector.');
+    transitionRadius = legacyRadius + transitionStep;
+    doubledRing = circle_ring(transitionRadius, zValue, ...
+        commonThetaCount, startAngle);
+    connectorPanels = one_to_two_annulus(legacyRing, doubledRing);
+    connectorAudit = audit_one_to_two_connector(connectorPanels);
+
+    remainingExtension = targetRadius - transitionRadius;
+    equalLayerCount = max(1, ceil(remainingExtension / radialTarget));
+    equalBlocks = cell(equalLayerCount, 1);
+    currentRing = doubledRing;
+    for layerIndex = 1:equalLayerCount
+        radius = transitionRadius + remainingExtension * ...
+            layerIndex / equalLayerCount;
+        nextRing = circle_ring(radius, zValue, commonThetaCount, startAngle);
+        equalBlocks{layerIndex} = zipper_annulus(currentRing, nextRing);
+        currentRing = nextRing;
+    end
+    panels = cat(1, connectorPanels, equalBlocks{:});
+    mesh = append_horizontal_panels(mesh, panels, panelType, cfg);
+    outerRing = currentRing;
+    radialLayerCount = 1 + equalLayerCount;
+    maximumRadialStep = max(transitionStep, ...
+        remainingExtension / equalLayerCount);
+    mesh.header = sprintf('%s | QUALITY_CONTROLLED_V2 r=%.9g Ntheta=%d', ...
+        mesh.header, targetRadius, commonThetaCount);
+    info = surface_info(0, size(panels, 1), radialLayerCount, ...
+        legacyCount, commonThetaCount, maximumRadialStep, ...
+        max_ring_edge(outerRing), radialTarget, thetaTarget, true, false);
+    info.ringThetaNodeCounts = [legacyCount; ...
+        repmat(commonThetaCount, equalLayerCount + 1, 1)];
+    info.gradualThetaTransition = false;
+    info.qualityControlledOneToTwo = true;
+    info.maximumAdjacentThetaCountRatio = 2.0;
+    info.oneToTwoTransitionRadiusM = transitionRadius;
+    info.oneToTwoTransitionStepM = transitionStep;
+    info.oneToTwoConnectorAudit = connectorAudit;
+    info.topology = 'quality-controlled-fixed-1to2-then-equal-count-quads';
+end
+
+function panels = one_to_two_annulus(innerRing, outerRing)
+% ONE_TO_TWO_ANNULUS Apply the predeclared three-triangle sector template.
+
+    innerCount = size(innerRing, 1);
+    outerCount = size(outerRing, 1);
+    assert(outerCount == 2 * innerCount, ...
+        'CRESTU:QualityPolarOneToTwoShape', ...
+        'The fixed connector requires exactly twice as many outer nodes.');
+    panels = zeros(3 * innerCount, 4, 3);
+    cursor = 0;
+    for innerIndex = 1:innerCount
+        nextInner = mod(innerIndex, innerCount) + 1;
+        outerFirst = 2 * innerIndex - 1;
+        outerMiddle = 2 * innerIndex;
+        outerLast = mod(2 * innerIndex, outerCount) + 1;
+        triangles = { ...
+            [innerRing(innerIndex, :); outerRing(outerFirst, :); ...
+                outerRing(outerMiddle, :)]; ...
+            [innerRing(innerIndex, :); outerRing(outerMiddle, :); ...
+                innerRing(nextInner, :)]; ...
+            [innerRing(nextInner, :); outerRing(outerMiddle, :); ...
+                outerRing(outerLast, :)]};
+        for triangleIndex = 1:3
+            triangle = triangles{triangleIndex};
+            cursor = cursor + 1;
+            panels(cursor, :, :) = reshape([triangle; triangle(3, :)], ...
+                1, 4, 3);
+        end
+    end
+end
+
+function audit = audit_one_to_two_connector(panels)
+% AUDIT_ONE_TO_TWO_CONNECTOR Enforce frozen angle/aspect/orientation gates.
+
+    panelCount = size(panels, 1);
+    minimumAngle = Inf;
+    maximumAspectRatio = 0.0;
+    minimumWindingAlignment = Inf;
+    for panelIndex = 1:panelCount
+        triangle = reshape(panels(panelIndex, 1:3, :), 3, 3);
+        next = [2, 3, 1];
+        edges = triangle(next, :) - triangle;
+        lengths = vecnorm(edges, 2, 2);
+        maximumAspectRatio = max(maximumAspectRatio, ...
+            max(lengths) / min(lengths));
+        for vertexIndex = 1:3
+            previousIndex = mod(vertexIndex - 2, 3) + 1;
+            incoming = triangle(previousIndex, :) - triangle(vertexIndex, :);
+            outgoing = triangle(next(vertexIndex), :) - triangle(vertexIndex, :);
+            cosine = dot(incoming, outgoing) / ...
+                (norm(incoming) * norm(outgoing));
+            minimumAngle = min(minimumAngle, ...
+                acosd(max(-1, min(1, cosine))));
+        end
+        winding = cross(triangle(2, :) - triangle(1, :), ...
+            triangle(3, :) - triangle(1, :));
+        winding = winding / norm(winding);
+        minimumWindingAlignment = min(minimumWindingAlignment, winding(3));
+    end
+    assert(minimumAngle >= 35.0 && maximumAspectRatio <= 12.0 && ...
+        minimumWindingAlignment >= 1 - 1.0e-12, ...
+        'CRESTU:QualityPolarOneToTwoQuality', ...
+        ['The fixed 1:2 connector violates a frozen quality gate: ', ...
+        'angle=%.9g deg, AR=%.9g, orientation=%.9g.'], ...
+        minimumAngle, maximumAspectRatio, minimumWindingAlignment);
+    audit = struct('panelCount', panelCount, ...
+        'minimumAngleDeg', minimumAngle, ...
+        'maximumAspectRatio', maximumAspectRatio, ...
+        'minimumWindingAlignment', minimumWindingAlignment, ...
+        'qualityGatePass', true, 'template', 'three-triangles-per-inner-sector');
+end
+
 function count = gradual_ring_count(legacyCount, finalCount, ...
         previousCount, layerIndex, layerCount)
 % GRADUAL_RING_COUNT Distribute explicit count changes across radial layers.
diff --git a/Source Code/2.Mesh/generate_free_surface_bmf.m b/Source Code/2.Mesh/generate_free_surface_bmf.m
index d5ef672..44d124f 100644
--- a/Source Code/2.Mesh/generate_free_surface_bmf.m
+++ b/Source Code/2.Mesh/generate_free_surface_bmf.m
@@ -49,6 +49,15 @@ function mesh_fs = generate_free_surface_bmf(filename, waterline, cfg, writeOutp
     is_closed = waterline.is_closed;
     nr_total = nr_near + nr_sponge;

+    if get_polar_topology_mode(cfg) == "QUALITY_CONTROLLED_V2"
+        mesh_fs = generate_quality_controlled_polar_surface( ...
+            waterline, cfg, 0.0, 2);
+        if writeOutput
+            write_bmf(filename, mesh_fs);
+        end
+        return
+    end
+
 % =====================================================================
 % =====================================================================
     seg_lens = zeros(n_pts, 1);
diff --git a/Source Code/2.Mesh/generate_quality_controlled_polar_surface.m b/Source Code/2.Mesh/generate_quality_controlled_polar_surface.m
new file mode 100644
index 0000000..50a24e7
--- /dev/null
+++ b/Source Code/2.Mesh/generate_quality_controlled_polar_surface.m
@@ -0,0 +1,145 @@
+function mesh = generate_quality_controlled_polar_surface( ...
+        waterline, cfg, zValue, panelType)
+% GENERATE_QUALITY_CONTROLLED_POLAR_SURFACE Build an explicit annular O-grid.
+% This helper is reachable only through POLAR_TOPOLOGY_MODE=QUALITY_CONTROLLED_V2.
+
+    %% Stage 1: Validate the explicit single-body/full-domain contract
+
+    assert(get_polar_topology_mode(cfg) == "QUALITY_CONTROLLED_V2", ...
+        'CRESTU:QualityPolarModeInactive', ...
+        'Quality-controlled polar generation requires its explicit mode.');
+    assert(cfg.n_bodies == 1 && cfg.isx == 0 && cfg.isy == 0 && ...
+        waterline.is_closed, 'CRESTU:QualityPolarScope', ...
+        'QUALITY_CONTROLLED_V2 currently requires one full-domain closed body.');
+    nodes = double(waterline.nodes); % [m]
+    thetaCount = size(nodes, 1);
+    assert(thetaCount >= 8 && mod(thetaCount, 8) == 0, ...
+        'CRESTU:QualityPolarThetaCount', ...
+        'Quality-controlled waterline count must be a multiple of eight.');
+    nodeRadius = hypot(nodes(:, 1), nodes(:, 2));
+    innerRadiusM = mean(nodeRadius); % [m]
+    assert(max(abs(nodeRadius - innerRadiusM)) <= 1.0e-6, ...
+        'CRESTU:QualityPolarAxisymmetry', ...
+        'QUALITY_CONTROLLED_V2 currently requires an axisymmetric waterline.');
+    transitionRadiusM = cfg.fs.r_inner; % [m]
+    if isfield(cfg, 'phase2_2_controls') && ...
+            isfield(cfg.phase2_2_controls, 'meshTransitionRadiusM')
+        transitionRadiusM = cfg.phase2_2_controls.meshTransitionRadiusM;
+    end
+    outerRadiusM = cfg.fs.r_outer; % [m]
+    assert(innerRadiusM < transitionRadiusM && ...
+        transitionRadiusM < outerRadiusM, ...
+        'CRESTU:QualityPolarRadii', ...
+        'Quality-controlled inner/transition/outer radii are invalid.');
+
+    %% Stage 2: Resolve predeclared radial targets without smoothing
+
+    nearLayerCount = cfg.fs.nr_near;
+    configuredOuterLayerCount = cfg.fs.nr_sponge;
+    transitionChordM = maximum_ring_edge( ...
+        circular_ring(nodes, transitionRadiusM, zValue)); % [m]
+    requestedOuterStepMaxM = 8.0 * transitionChordM; % [m]
+    requestedOuterLayerCount = ceil( ...
+        (outerRadiusM - transitionRadiusM) / requestedOuterStepMaxM);
+    outerLayerCount = max(configuredOuterLayerCount, ...
+        requestedOuterLayerCount);
+    nearRadii = linspace(innerRadiusM, transitionRadiusM, ...
+        nearLayerCount + 1);
+    outerRadii = linspace(transitionRadiusM, outerRadiusM, ...
+        outerLayerCount + 1);
+    ringRadii = [nearRadii, outerRadii(2:end)]; % [m]
+    radialLayerCount = numel(ringRadii) - 1;
+    realizedNearStepM = (transitionRadiusM - innerRadiusM) / ...
+        nearLayerCount;
+    realizedOuterStepM = (outerRadiusM - transitionRadiusM) / ...
+        outerLayerCount;
+
+    %% Stage 3: Build equal-count orthogonal annular rings
+
+    rings = cell(numel(ringRadii), 1);
+    rings{1} = [nodes, repmat(zValue, thetaCount, 1)];
+    for ringIndex = 2:numel(ringRadii)
+        rings{ringIndex} = circular_ring(nodes, ringRadii(ringIndex), zValue);
+    end
+    panelCount = radialLayerCount * thetaCount;
+    vertices = zeros(panelCount, 4, 3);
+    centers = zeros(panelCount, 3);
+    normals = repmat([0, 0, 1], panelCount, 1);
+    areas = zeros(panelCount, 1);
+    e1 = zeros(panelCount, 3);
+    e2 = zeros(panelCount, 3);
+    muDamping = zeros(panelCount, 1);
+    cursor = 0;
+    for ringIndex = 1:radialLayerCount
+        innerRing = rings{ringIndex};
+        outerRing = rings{ringIndex + 1};
+        for thetaIndex = 1:thetaCount
+            nextTheta = mod(thetaIndex, thetaCount) + 1;
+            cursor = cursor + 1;
+            panel = [innerRing(thetaIndex, :); outerRing(thetaIndex, :); ...
+                outerRing(nextTheta, :); innerRing(nextTheta, :)];
+            vertices(cursor, :, :) = reshape(panel, 1, 4, 3);
+            centers(cursor, :) = mean(panel, 1);
+            areaVector = cross(panel(3, :) - panel(1, :), ...
+                panel(4, :) - panel(2, :));
+            assert(areaVector(3) > 0, 'CRESTU:QualityPolarOrientation', ...
+                'A quality-controlled horizontal panel is reversed.');
+            areas(cursor) = 0.5 * norm(areaVector);
+            radialVector = mean(panel(2:3, :), 1) - mean(panel([1, 4], :), 1);
+            e1(cursor, :) = radialVector / norm(radialVector);
+            e2(cursor, :) = cross(normals(cursor, :), e1(cursor, :));
+            centerRadiusM = hypot(centers(cursor, 1), centers(cursor, 2));
+            if centerRadiusM > transitionRadiusM
+                normalizedRadius = (centerRadiusM - transitionRadiusM) / ...
+                    (outerRadiusM - transitionRadiusM);
+                muDamping(cursor) = cfg.fs.mu0 * normalizedRadius ^ 2;
+            end
+        end
+    end
+
+    %% Stage 4: Persist requested/realized topology metadata
+
+    innerChordM = maximum_ring_edge(rings{1});
+    outerChordM = maximum_ring_edge(rings{end});
+    mesh = struct('header', sprintf( ...
+        'QUALITY_CONTROLLED_V2 polar surface Ntheta=%d Nnear=%d Nouter=%d', ...
+        thetaCount, nearLayerCount, outerLayerCount), ...
+        'ulen', 1.0, 'panel_type', repmat(panelType, panelCount, 1), ...
+        'isx', cfg.isx, 'isy', cfg.isy, 'n_panels', panelCount, ...
+        'vertices', vertices, 'centers', centers, 'normals', normals, ...
+        'areas', areas, 'e1', e1, 'e2', e2, ...
+        'mu_damping', muDamping, ...
+        'hydrostatics', struct('Vx', 0, 'Vy', 0, 'Vz', 0, ...
+            'V_mean', 0, 'center_of_buoyancy', [0, 0, 0]), ...
+        'quality_controlled_polar', struct('schemaVersion', 1, ...
+            'mode', 'QUALITY_CONTROLLED_V2', ...
+            'thetaNodeCount', thetaCount, ...
+            'nearRadialLayerCount', nearLayerCount, ...
+            'configuredOuterRadialLayerCount', configuredOuterLayerCount, ...
+            'requestedOuterRadialLayerCount', requestedOuterLayerCount, ...
+            'realizedOuterRadialLayerCount', outerLayerCount, ...
+            'innerRadiusM', innerRadiusM, ...
+            'transitionRadiusM', transitionRadiusM, ...
+            'outerRadiusM', outerRadiusM, ...
+            'requestedNearRadialStepM', ...
+                (transitionRadiusM - innerRadiusM) / nearLayerCount, ...
+            'realizedNearRadialStepM', realizedNearStepM, ...
+            'requestedOuterRadialStepMaxM', requestedOuterStepMaxM, ...
+            'realizedOuterRadialStepM', realizedOuterStepM, ...
+            'innerChordM', innerChordM, ...
+            'transitionChordM', transitionChordM, ...
+            'outerChordM', outerChordM, ...
+            'ringRadiiM', ringRadii, ...
+            'smoothingApplied', false));
+end
+
+function ring = circular_ring(waterlineNodes, radiusM, zValue)
+    theta = atan2(waterlineNodes(:, 2), waterlineNodes(:, 1));
+    ring = [radiusM * cos(theta), radiusM * sin(theta), ...
+        repmat(zValue, numel(theta), 1)];
+end
+
+function value = maximum_ring_edge(ring)
+    next = ring([2:end, 1], :);
+    value = max(vecnorm(next - ring, 2, 2));
+end
diff --git a/Source Code/2.Mesh/generate_seabed_mesh.m b/Source Code/2.Mesh/generate_seabed_mesh.m
index ae8bc94..a06ec13 100644
--- a/Source Code/2.Mesh/generate_seabed_mesh.m
+++ b/Source Code/2.Mesh/generate_seabed_mesh.m
@@ -117,6 +117,19 @@ function mesh_seabed = generate_seabed_mesh(waterline, cfg, mesh_fs)
 % =====================================================================
 % =====================================================================
     Nr = max(3, round((Nx + Ny) * 0.25));
+    requestedQualityNr = 0;
+    if get_polar_topology_mode(cfg) == "QUALITY_CONTROLLED_V2"
+        assert(isfield(cfg.phase2_2_controls, ...
+            'bottomCoreRadialLayerCount'), ...
+            'CRESTU:QualityBottomCoreControlMissing', ...
+            'QUALITY_CONTROLLED_V2 requires BOTTOM_CORE_RADIAL_LAYERS.');
+        requestedQualityNr = ...
+            cfg.phase2_2_controls.bottomCoreRadialLayerCount;
+        assert(requestedQualityNr >= 1 && fix(requestedQualityNr) == ...
+            requestedQualityNr, 'CRESTU:QualityBottomCoreControl', ...
+            'BOTTOM_CORE_RADIAL_LAYERS must be a positive integer.');
+        Nr = requestedQualityNr;
+    end
     radialBlend = linspace(0.0, 1.0, Nr + 1);

 % Block 1
@@ -211,6 +224,17 @@ function mesh_seabed = generate_seabed_mesh(waterline, cfg, mesh_fs)
     mesh_seabed.e1 = e1;
     mesh_seabed.e2 = e2;
     mesh_seabed.hydrostatics = struct('Vx', 0,'Vy', 0,'Vz', 0,'V_mean', 0,'center_of_buoyancy', [0, 0, 0]);
+    mesh_seabed.quality_controlled_bottom_core = struct( ...
+        'topologyMode', char(get_polar_topology_mode(cfg)), ...
+        'boundarySegmentCountX', Nx, 'boundarySegmentCountY', Ny, ...
+        'legacyRadialLayerCount', max(3, round((Nx + Ny) * 0.25)), ...
+        'requestedRadialLayerCount', requestedQualityNr, ...
+        'realizedRadialLayerCount', Nr, ...
+        'sideBlockPanelCount', 2 * Nx * Nr + 2 * Ny * Nr, ...
+        'centralPatchPanelCount', Nx * Ny, ...
+        'totalStructuredCorePanelCount', ...
+            2 * Nx * Nr + 2 * Ny * Nr + Nx * Ny, ...
+        'singleCenterFan', false, 'boundaryNodeContractChanged', false);
 end

 % -------------------------------------------------------------------------
diff --git a/Source Code/2.Mesh/get_polar_topology_mode.m b/Source Code/2.Mesh/get_polar_topology_mode.m
new file mode 100644
index 0000000..4a7c4c0
--- /dev/null
+++ b/Source Code/2.Mesh/get_polar_topology_mode.m
@@ -0,0 +1,15 @@
+function mode = get_polar_topology_mode(cfg)
+% GET_POLAR_TOPOLOGY_MODE Resolve the explicit polar-mesh topology mode.
+% Missing fields always preserve the historical LEGACY production path.
+
+    mode = "LEGACY";
+    if isfield(cfg, 'phase2_2_controls') && ...
+            isfield(cfg.phase2_2_controls, 'polarTopologyMode') && ...
+            ~isempty(cfg.phase2_2_controls.polarTopologyMode)
+        mode = upper(string(cfg.phase2_2_controls.polarTopologyMode));
+    end
+    assert(isscalar(mode) && any(mode == ...
+        ["LEGACY", "QUALITY_CONTROLLED_V2"]), ...
+        'CRESTU:PolarTopologyModeValue', ...
+        'Polar topology mode must be LEGACY or QUALITY_CONTROLLED_V2.');
+end
diff --git a/Source Code/2.Mesh/resolve_phase2_2_controls.m b/Source Code/2.Mesh/resolve_phase2_2_controls.m
index b047744..b211442 100644
--- a/Source Code/2.Mesh/resolve_phase2_2_controls.m
+++ b/Source Code/2.Mesh/resolve_phase2_2_controls.m
@@ -9,6 +9,7 @@ function [cfg, manifest] = resolve_phase2_2_controls(cfg, omega)
         'Validated configuration lacks Phase-2.2 control defaults.');
     controls = cfg.phase2_2_controls;
     controls = add_theta_control_defaults(controls);
+    controls = add_polar_topology_default(controls);
     cfg.phase2_2_controls = controls;
     [waveNumber, ~] = solve_wave_dispersion(omega, cfg.grav, cfg.water_depth);
     wavelength = 2 * pi / waveNumber;
@@ -42,12 +43,15 @@ function [cfg, manifest] = resolve_phase2_2_controls(cfg, omega)
     independentInputs = struct( ...
         'fsRadialCounts', controls.fsRadialCounts, ...
         'bottomRadialCounts', controls.bottomRadialCounts, ...
+        'bottomCoreRadialLayerCount', ...
+            optional_bottom_core_layers(controls), ...
         'outerVerticalPanelCount', controls.outerVerticalPanelCount, ...
         'outerThetaPanelCount', controls.outerThetaPanelCount, ...
         'freeSurfaceOuterThetaPanelCount', ...
             controls.freeSurfaceOuterThetaPanelCount, ...
         'bottomOuterThetaPanelCount', ...
             controls.bottomOuterThetaPanelCount, ...
+        'polarTopologyMode', controls.polarTopologyMode, ...
         'meshTransitionRadiusM', controls.meshTransitionRadiusM, ...
         'spongeStartRadiusM', controls.spongeStartRadiusM, ...
         'spongeWidth', struct('mode', controls.spongeWidthMode, ...
@@ -66,12 +70,15 @@ function [cfg, manifest] = resolve_phase2_2_controls(cfg, omega)
         'meshTransitionRadiusM', controls.meshTransitionRadiusM, ...
         'fsRadialCounts', controls.fsRadialCounts, ...
         'bottomRadialCounts', controls.bottomRadialCounts, ...
+        'bottomCoreRadialLayerCount', ...
+            optional_bottom_core_layers(controls), ...
         'outerVerticalPanelCount', controls.outerVerticalPanelCount, ...
         'outerThetaPanelCount', controls.outerThetaPanelCount, ...
         'freeSurfaceOuterThetaPanelCount', ...
             controls.freeSurfaceOuterThetaPanelCount, ...
         'bottomOuterThetaPanelCount', ...
             controls.bottomOuterThetaPanelCount, ...
+        'polarTopologyMode', controls.polarTopologyMode, ...
         'spongeStartRadiusM', startRadius, ...
         'spongeWidthM', width, ...
         'spongeEndRadiusM', startRadius + width, ...
@@ -97,6 +104,33 @@ function [cfg, manifest] = resolve_phase2_2_controls(cfg, omega)
     cfg.phase2_2_controls.effective = effective;
 end

+function value = optional_bottom_core_layers(controls)
+    value = 0;
+    if isfield(controls, 'bottomCoreRadialLayerCount') && ...
+            ~isempty(controls.bottomCoreRadialLayerCount)
+        value = controls.bottomCoreRadialLayerCount;
+    end
+    assert(isnumeric(value) && isscalar(value) && isfinite(value) && ...
+        value >= 0 && fix(value) == value, ...
+        'CRESTU:BottomCoreRadialLayerCount', ...
+        'Bottom core radial layer count must be zero or a positive integer.');
+end
+
+function controls = add_polar_topology_default(controls)
+% ADD_POLAR_TOPOLOGY_DEFAULT Preserve every historical/in-memory caller.
+
+    if ~isfield(controls, 'polarTopologyMode') || ...
+            isempty(controls.polarTopologyMode)
+        controls.polarTopologyMode = 'LEGACY';
+    end
+    mode = upper(string(controls.polarTopologyMode));
+    assert(isscalar(mode) && any(mode == ...
+        ["LEGACY", "QUALITY_CONTROLLED_V2"]), ...
+        'CRESTU:PolarTopologyModeValue', ...
+        'Polar topology mode must be LEGACY or QUALITY_CONTROLLED_V2.');
+    controls.polarTopologyMode = char(mode);
+end
+
 function controls = add_theta_control_defaults(controls)
 % ADD_THETA_CONTROL_DEFAULTS Preserve old in-memory audit fixtures.
 % Zero means that the endpoint count is not an independent configured input;
diff --git a/Source Code/4.Potential/get_rankine_code_version.m b/Source Code/4.Potential/get_rankine_code_version.m
index e8fbdac..a145832 100644
--- a/Source Code/4.Potential/get_rankine_code_version.m
+++ b/Source Code/4.Potential/get_rankine_code_version.m
@@ -16,6 +16,8 @@ function codeVersion = get_rankine_code_version()
         fullfile('Source Code', '1.Input', 'read_config.m'), ...
         fullfile('Source Code', '2.Mesh', 'build_bmf_domain.m'), ...
         fullfile('Source Code', '2.Mesh', 'generate_free_surface_bmf.m'), ...
+        fullfile('Source Code', '2.Mesh', ...
+            'generate_quality_controlled_polar_surface.m'), ...
         fullfile('Source Code', '2.Mesh', 'generate_multibody_free_surface_bmf.m'), ...
         fullfile('Source Code', '2.Mesh', 'generate_seabed_mesh.m'), ...
         fullfile('Source Code', '2.Mesh', 'generate_seabed_disk_mesh.m'), ...
@@ -28,6 +30,7 @@ function codeVersion = get_rankine_code_version()
         fullfile('Source Code', '2.Mesh', 'merge_domain_geometry.m'), ...
         fullfile('Source Code', '2.Mesh', 'tune_sponge_layer.m'), ...
         fullfile('Source Code', '2.Mesh', 'apply_phase2_2_mesh_controls.m'), ...
+        fullfile('Source Code', '2.Mesh', 'get_polar_topology_mode.m'), ...
         fullfile('Source Code', '2.Mesh', 'get_phase2_2_bottom_mesh_inputs.m'), ...
         fullfile('Source Code', '2.Mesh', 'resolve_phase2_2_controls.m'), ...
         fullfile('Source Code', '2.Mesh', 'finalize_phase2_2_manifest.m'), ...
