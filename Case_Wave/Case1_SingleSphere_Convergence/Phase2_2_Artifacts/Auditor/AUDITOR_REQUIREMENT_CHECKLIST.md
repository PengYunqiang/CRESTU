# Phase 2.2 Auditor Requirement Checklist

> **Artifact class:** independent audit metadata; not production code, solver configuration, test implementation, or Executor output.
>
> **Owner:** Agent B — Auditor. Production-code edits are prohibited for this owner.
>
> **Status legend:** `PENDING`, `PASS`, `WARNING`, `BLOCKER`, `N/A`. A checkpoint cannot pass while any in-scope item is `BLOCKER`. Every blocker must be cleared by Executor evidence before final acceptance.

## 0. Audit control

| ID | Requirement / acceptance evidence | Status | Evidence / note |
|---|---|---|---|
| AC-01 | Preserve the confirmed Phase 2.1 fixes: separated body/outer studies; geometry-bound result/potential cache keys; raw/projected/energy separation; RAO not overwritten by energy/Haskind; clean/reload machine agreement. Do not reopen or alter them without new evidence. | PASS | Previous report read; Executor explicitly freezes these areas. Any later diff touching them reopens this item. |
| AC-02 | Scope is single-sphere, first-order Rankine Phase 2.2. No multi-body, roll-specialist, second-order drift/QTF work. | PASS | Executor plan is confined to the requested single-sphere first-order scope. |
| AC-03 | Executor alone may edit production code. Auditor may update only independent audit metadata/review. | PASS | This file is isolated under `Phase2_2_Artifacts/Auditor/`. |
| AC-04 | Auditor reviews requirements, plan, critical diffs/formulae/configurations, decisive numerical evidence, test validity, final report; it does not duplicate full-repo review or large computations. | PASS | Review protocol established below. |
| AC-05 | Conclusions must identify unresolved blockers honestly; `PARTIAL` is required where evidence is partial. Phase may be declared complete only with `FINAL_AUDIT = PASS`. | PENDING | |

## 1. Baseline and experimental controls

| ID | Requirement / acceptance evidence | Status | Evidence / note |
|---|---|---|---|
| BL-01 | Read current project/tests/latest `NUMERICAL_AUDIT_REPORT.md`; continue from confirmed results rather than restarting or refactoring validated areas. | PASS | Auditor read latest report. Executor reports reading the report plus relevant single-sphere solver/mesh/BC/force/cache/test call chain and continues from confirmed fixes. |
| BL-02 | Fix the current Fine body mesh throughout Phase 2.2 diagnostics. Do not change body geometry, body-pressure integration, basic RAO definition, verified cache fingerprint, or raw/projected separation. | PENDING | All clean comparisons retain the 588-panel Fine body and identical body/component/merged/source/collocation hashes. The only pressure-path change is the specifically proven body-versus-fluid traction sign at the final generalized integral; pressure samples, areas, normals, quadrature, A/B mapping, RAO and raw/projected separation are unchanged. This evidence-supported exception remains subject to final Phase review. |
| BL-03 | Baseline includes at least one low-, one mid-, and at least three representative `omega >= 1.5 rad/s` frequencies, with actual values stated. | PASS | Clean pre/post evidence exists at omega = {0.5, 1.1, 1.5, 1.7, 2.0}, providing one low, one mid and three high-frequency points. |
| BL-04 | Every decisive diagnostic uses clean recompute and records cache miss; old cache is not used to infer root cause. Cache reload may only be a separate reproducibility check. | PENDING | The orientation comparison passes this control: ten newly absent isolated runs, `IPOTEN=1`, observed `MISS` and `forced_recompute`. The same evidence remains mandatory for every later decisive study. |
| BL-05 | Save a pre-change baseline before each evidence-supported production fix and retain before/after comparable data. | PENDING | The first orientation change now has an isolated pre snapshot and five-frequency paired raw before/after artifacts. This is a continuing requirement for every later production fix. |

## 2. Outer-domain OFAT and geometry authenticity

| ID | Requirement / acceptance evidence | Status | Evidence / note |
|---|---|---|---|
| OD-01 | A genuine OFAT design varies exactly one of: FS panel spacing; FS radial extent; bottom panel spacing; bottom radial extent; outer-vertical-boundary spacing; outer radius; sponge/radiation-zone start; zone width; current sponge/radiation parameters. Other factors are demonstrably fixed. | BLOCKER | Common-radius factor 6 now has a genuine held-resolution Core-A geometry design. Existing top/bottom-only frustum rows remain supplemental truncation-shape diagnostics because closure retessellates the fixed endpoint. Meeting explicit factors 2/4 requires the proposed P-full independent top/bottom endpoint azimuthal counts and unequal-count wall zipper (or an equivalent design) so the fixed endpoint mesh hash/count is bitwise invariant. P-min common theta control alone cannot close this item. |
| OD-02 | Each run records body/FS/bottom/outer panel counts, total unknowns, component/whole mesh SHA/hash, cache hit/miss, frequency, wavelength, and matrix dimensions. | PENDING | All fields are in the plan; actual rows pending. Whole hash must include BC-affecting non-geometric parameters or be accompanied by a configuration/BC hash. |
| OD-03 | Configuration changes are proven to alter the intended active solver geometry/BC only; no fake experiment from unchanged mesh, wrong object, stale cache, rounded-away setting, or wrong execution path. | PENDING | The instrumentation gate now hashes the actual FS radii/damping/complex Robin coefficients, outer gamma and a geometry-plus-BC boundary-operator state. Start/width/mu0/auto-tune produce `ACTUAL_OPERATOR_CHANGED`; diagnostics toggles preserve the operator as `NO_EFFECT_EXPECTED`; policy-only with unchanged actual mu/operator is rejected as `NO_EFFECT/NOT_A_SCIENTIFIC_LEVEL` (`reportPass=0`); two-field co-variation is rejected. FS radial, bottom radial, outer vertical and transition component-hash whitelists remain valid. This clears the instrumentation blocker; each real sweep row must still prove the same run-level contracts. |
| OD-04 | Radius and resolution are not conflated into a generic Coarse/Medium/Fine convergence claim. | BLOCKER | Core-A passes this design control: all 15 common-radius meshes hold every audited directional spacing within 5%, with no finer exception, while changing FS/bottom/outer/merged hashes. But B/C are still only radial+vertical under current controls, and top/bottom-only extents retain fixed-end retessellation. Full orthogonal and individual-extent claims remain blocked until P-full preflight/CP4 passes. |
| OD-05 | Radius × Resolution A: hold `h/lambda` approximately constant while increasing outer radius/lambda; quantify how closely resolution is held. | WARNING | Geometry-only production-path preflight passes 15/15 at five frequencies. `q=(R-20)/lambda`; three levels add exactly 0/1/2 radial layers, retain cylindrical top=bottom radius, derive theta counts 56 to at most 120, keep all audited directional spacings within +/-5%, have no finer exceptions, preserve body hash and change every outer component hash. Total `delta R/lambda` is 0.97–1.82. This is an adequate radius-sensitivity screen, not a convergence proof; a non-plateau/oscillatory BEM result must trigger a farther noncommensurate level or an explicit `NOT_CONVERGED` verdict. |
| OD-06 | Radius × Resolution B: hold geometric extent/radius fixed while changing `h/lambda`; prove identical radii/hashes except intended remeshing. | BLOCKER | Current controls cannot refine azimuthal/tangential resolution independently of the fixed 56-node interface, so the proposed B vector is only radial+vertical and cannot answer full directional wave resolution. P-full is required, followed by exact no-rounding/no-op preflight of every B level at all five frequencies. |
| OD-07 | Radius × Resolution C: improve radius and resolution together by an explicit stated ratio; interpret only as mixed-error evidence. | BLOCKER | C cannot be an all-direction resolution/radius study until the P-full azimuthal controls exist. After P-full, each paired level must prove monotone actual directional `h/lambda`, stated radius increments, N<=resource cap and unique geometry/cache hashes; it remains mixed-error evidence and cannot substitute for A or B. |
| OD-08 | Do not call movement toward WAMIT “convergence”; convergence must be supported by successive self-consistent numerical refinements and asymptotic behavior/error evidence. | PENDING | |

## 3. Wavelength-normalized resolution

| ID | Requirement / acceptance evidence | Status | Evidence / note |
|---|---|---|---|
| WR-01 | Dispersion relation and computed wavelength are stated (including finite depth as applicable), traceable, and dimensionally consistent. | PENDING | Finite-depth dispersion explicitly planned; derivation/source and results pending. |
| WR-02 | Report `h/L_body`, `h/lambda`, `lambda/h` (panels per wavelength), `R_outer/lambda`, `R_FS/lambda`, and `R_bottom/lambda` for relevant components/runs. | PENDING | Design reports component/band radial, tangential and vertical scales plus P95/max/aspect/skew rather than an aggregate only. Results pending. |
| WR-03 | Explicitly test whether nominal “Fine” becomes under-resolved as omega rises and lambda falls. | PENDING | Design audit estimates worst directional high-frequency resolution at only about 1.23–3.18 panels/lambda, a credible under-resolution hypothesis. It is not yet a causal conclusion. |
| WR-04 | Deliver high-frequency `quantity vs h/lambda` and `error/sensitivity vs h/lambda` data and figures, using unsmoothed raw physical values. | PENDING | |

## 4. Boundary-condition mathematical audit

| ID | Requirement / acceptance evidence | Status | Evidence / note |
|---|---|---|---|
| BC-01 | Declare whether code uses `exp(+i omega t)` or `exp(-i omega t)` and use one convention consistently in all derivations and labels. | PASS | Incident `exp(-ikx)` propagates in +x under `exp(+iωt)`; `Phi_I=i g a/ω V(z) exp(-ikx)` and `p=-iρ ω Phi` are consistent for peak amplitude `a`. |
| BC-02 | For radiation body Neumann, diffraction body Neumann, FS Robin, bottom Neumann, outer outgoing-wave BC, radiation potential→pressure→A/B, and diffraction potential→excitation: provide theory → time convention → normal definition → exact code location mapping. | PASS | The cylindrical chain remains accepted. The corrected approved-Fine frustum has stored inward normals with negligible tangential leakage (`1.95e-15`), an actual N=2752 production `D+gamma*S` column with zero reconstruction error, a clean diffraction residual `1.00e-13`, complete gamma/geometry/cache manifests, closed topology and independently checked zipper integration. This clears the frustum mathematical/execution gate. It does not by itself make the q sweep a pure radius OFAT. |
| BC-03 | Independently trace `+iω/-iω`, incident phase, unit-displacement versus unit-velocity radiation normalization, wave-amplitude normalization, diffraction RHS sign, and real/imag mapping to A/B. | PASS | The consistent chain is now exercised on radiation and diffraction paths: `q_s=iωn_j`, radiation `rhs=-S q_s`; fixed-body `q_D=-q_I` and diffraction `rhs=+S q_I`; `p=-iρωPhi`; `Q=-integral(p n_B)`; unchanged `A=Re(Q)/ω²`, `B=-Im(Q)/ω`. T8 finite-depth incident Phi/q and peak-amplitude phase close to machine precision, while opposite RHS/traction controls fail by about 199%. |
| BC-04 | Define body, source-panel, collocation, and outer-boundary normal orientations and verify implementation signs/self terms against those definitions. | PASS | Production now canonicalizes only double-layer source derivatives to `n_D=-n_Omega` with signs `[body +1, FS -1, bottom +1, outer +1]`, asserts stored normal/winding alignment, leaves S/RHS/physical normals/self 0.5 unchanged, and exposes the contract. T2 mixed residual improved `0.21795 -> 5.55e-17`. Collocation has no normal. Other BC defects remain separate blockers. |
| BC-05 | Audit Sommerfeld `ik` sign, radial-decay term, finite-depth dispersion/depth factor, and sponge/radiation-zone implementation. | BLOCKER | T3 physical FS, the approved non-exact T5 first-order local outer ABC, and its projected active-frustum implementation now pass focused production-path tests. The item remains blocked at Phase level only because the dynamic sponge rule/profile and its OFAT/convergence behavior have not yet been validated. Actual outermost FS collocation-ring mu is only `0.611–0.885` versus nominal `1.273`; this exposed discretization/profile coupling must be analyzed, not hidden. |
| BC-06 | Any convention-mixing claim is first demonstrated by a minimal reproduction/unit test before production modification. | PASS | T1–T8/T10 pass the cylindrical production contracts. The frustum subsequently received its focused topology/normal/gamma/assembly gate on the approved Fine object, including an actual clean production solve and sensitive negative controls. No convention change was accepted from inspection alone. |

## 5. Three independent damping closures and dimensional audit

| ID | Requirement / acceptance evidence | Status | Evidence / note |
|---|---|---|---|
| DP-01 | Persist independent per-frequency `B_pressure` from radiation body-pressure integration; it is not overwritten by other paths. | PASS | `B_pressure_raw` is copied from the raw body-pressure path and is neither overwritten by control-surface flux nor the Haskind candidate. The clean w=1.5 diagnostics gate preserves physical solver arrays bitwise. Numerical convergence of this formula remains a separate final-study question. |
| DP-02 | Persist independent per-frequency `B_flux` from radiated-wave/far-field energy flux; it does not alias pressure or Haskind data. | WARNING | A genuinely independent control-cylinder pressure-work path now exists and retains raw complex work, unsymmetrized damping and real Hermitian presentation. At w=1.5 it is correctly `NOT_VALIDATED`: B33 changes `78183.7 -> 62240.4 -> 59726.1` across radii (23.6% plateau residual) and the unsymmetrized Hermitian residual is 70.8%. Implementation independence passes; numerical flux validation does not. |
| DP-03 | Persist independent per-frequency `B_haskind` only when theory and normalization are confirmed; otherwise explicitly label `NOT VALIDATED` (or final `NOT_APPLICABLE`) without substitution. | PASS | Schema 7, the active frequency runner, benchmark, convergence consumer and report helpers now use Haskind-candidate-specific fields and retain `NOT_VALIDATED/isHaskindCandidate=true/isEnergyFlux=false`; the old function is a deprecated wrapper and schema-6 mapping is explicitly historical/not B_flux. The executable consumer and shared legacy-token scanner negatives pass. This status accepts semantic independence only; Haskind formula/prefactor validation remains blocked under DP-05/DP-07. |
| DP-04 | Output `B_pressure`, `B_flux`, `B_haskind`, and all three pairwise ratios by frequency, with invalid/zero denominators handled transparently. | PASS | `damping_closure` stores the three distinct numerical arrays/candidate and all pairwise/B33 ratios; zero/nonfinite denominators become NaN and every ratio carries a validation-status field. Invalid flux/Haskind values remain visible but are not promoted. |
| DP-05 | Audit each damping prefactor/normalization term: `rho`, `g`, `omega`, `k`, phase/group velocity, finite-depth factor, `tanh(kh)`, amplitude basis, displacement/velocity basis, peak/RMS, complex convention, factors 2/pi/2pi, angular integration. | BLOCKER | The flux prefactor/sign now passes an independent finite-depth plane-wave oracle: with `exp(+iωt)`, peak amplitudes and unit displacement, `p=-iρ ω Phi`, `P_out=1/2 Re integral(p q_out*)`, `B=2P_out/ω²`; the z=16 group-velocity error is `4.21e-15` and reversed normal gives negative power. For cross modes, the stored raw index convention is transposed relative to the usual `xi^H C xi`, but `real(Hermitian(C))` is invariant under that transpose and is the correct real symmetric damping presentation; raw complex work remains available. Haskind applicability, angular prefactor and normalization remain unvalidated, so the full item stays blocked. |
| DP-06 | Provide a B33 dimensional derivation/check; numerical agreement alone is not validation. | PENDING | Required derivation must include division by generalized displacement amplitudes: `A=Re(Q)/(ω² ξ)`, `B=-Im(Q)/(ω ξ)` and `B_flux=Re(C)/(ω² ξ_i ξ_j*)`; solver currently assumes numerical 1 m/1 rad without unit-carrying interfaces. |
| DP-07 | Determine which closure first loses agreement and separate a faulty diagnostic formula from a faulty solver BC/solution. | BLOCKER | Existing `compute_radiation_damping_energy` is a Haskind-force outer product, not direct flux. Current Haskind body integrand uses a plus where the same-normal Green concomitant is antisymmetric (`Phi_I q_R - Phi_R q_I`); it is `NOT VALIDATED` until a manufactured identity fixes sign, conjugation, incident direction and prefactor. |

## 6. Raw physics diagnostics and conditioning

| ID | Requirement / acceptance evidence | Status | Evidence / note |
|---|---|---|---|
| RD-01 | For every representative frequency save `A_raw`, `B_raw`, raw reciprocity residual, diagonal B, symmetric B, and `min eig(sym(B_raw))`; projected values do not hide raw behavior. | PENDING | Instrumentation and a manufactured oracle now retain raw A/B diagonals, `sym(B_raw)` and its minimum eigenvalue without changing extraction; the required values must still be populated and audited for every representative sweep frequency. |
| RD-02 | Save radiation and diffraction linear residuals, solution norm, maximum radiation potential, maximum FS potential, and outer-boundary potential magnitude. | PENDING | Full-vector lifecycle/provenance is corrected. `PERSIST_FULL_PHI=1` with `IPOTEN=0` now hard-stops in `read_config` before domain generation/output; a real compatible-cache negative preserves RawDiagnostics/cache/results bytes and SHA. Every full record carries entry cache key, configured/effective SHA, code fingerprint, matrix size, all component/merged/source/collocation hashes. The current Fine w=1.5 clean gate passes; all representative sweep frequencies and independent off-surface physical BC residuals remain pending. |
| RD-03 | Save BC residual statistics separately for body, FS, bottom, and outer boundary; define norm/statistics and normalization. No global-only residual. | PENDING | Independent off-surface Green reconstruction now supplies radiation/diffraction body/FS/bottom/outer absolute and normalized median/P95/max plus epsilon sensitivity. At the w=1.5 gate all normalized P95 values are <=2.90% and epsilon sensitivity P95 <=0.869%. `VALIDATED_AT_SELECTED_EPSILON` is accepted only as a narrowly scoped computational-status label: the 25% gate is permissive, normalization is a per-column global scale, and constant-panel near-singular reconstruction is not a continuum BC proof. The same records are still required at every representative frequency before this item passes. |
| RD-04 | Record a reliable matrix condition estimate if algorithmically feasible; if infeasible, state method limitation and use clearly labelled proxy evidence rather than inventing a condition number. | PENDING | Semantics and calibration now pass: fields are `EstimateAvailable`, not “reliable”; exact diagonal rcond 0.125 and near-singular `1e-10` are reproduced, while a zero-row singular matrix returns 0/UNAVAILABLE. Metadata states raw/scaled dense 1-norm estimates and forbids treating equilibration improvement as physical accuracy/convergence. Per-sweep estimates and correlation with sensitivity remain pending. |
| RD-05 | Correlate conditioning/residual/solution/potential diagnostics with panel count, radius, `h/lambda`, and B33/F3/RAO3 sensitivity. | PENDING | |
| RD-06 | If refinement worsens results, discriminate among non-asymptotic mesh, conditioning, BC error, quadrature/panel-integral error, wave under-resolution, and boundary reflection; do not assume Medium or Fine is correct. | PENDING | |

## 7. WAMIT validation and forbidden shortcuts

| ID | Requirement / acceptance evidence | Status | Evidence / note |
|---|---|---|---|
| WV-01 | WAMIT is independent validation only, never a tuning/selection target. CRESTU and WAMIT remain unscaled raw physical values. | PENDING | |
| WV-02 | Each comparison includes A33, B33, `|F3|`, F3 phase, RAO amplitude, and RAO phase where available—not amplitude alone. | PENDING | |
| FS-01 | No frequency-dependent empirical correction, WAMIT-based scale, artificial multiplier, smoothing, curve fit, damping tuned for RAO, or plotting-data manipulation. | PENDING | |
| FS-02 | No reciprocity projection to conceal raw-solver defects; no PSD clipping to conceal negative damping. | PENDING | |
| FS-03 | No prohibited scope diversion or body-mesh refinement as the proposed cure. | PENDING | |

## 8. Production-change evidence gate

| ID | Requirement / acceptance evidence | Status | Evidence / note |
|---|---|---|---|
| CH-01 | Forensic audit precedes any production change; every change identifies one specific physical/numerical defect with theoretical or direct numerical evidence. | PASS | Frustum instrumentation follows the approved need to vary top/bottom physical truncation radii while preserving a closed Green domain. It does not use WAMIT or empirical correction. |
| CH-02 | One physical issue is changed at a time; no bundled unrelated fixes that prevent attribution. | PASS | The focused diff adds optional q input, closure geometry, hashes/cache and assembly audit while leaving the established ABC/FS/sponge/RHS/force/closure/RAO formulas unchanged. The remediation changes mesh qualification/instrumentation only. |
| CH-03 | Each change records exact code location, why old implementation is wrong, governing formula, and a focused valid unit/minimal test that would fail before and pass after. | PASS | The resubmission replaces each false-green predicate: symmetric resolution classification has a rejected-coarse control; self-intersection uses a real triangle narrow phase with a folded-wall negative; final-vertex metrics replace requested/construction surrogates; worst zipper quadrature has an independent Duffy oracle. The approved-Fine active production column and solve exercise the changed path. |
| CH-04 | Each change reruns representative clean frequencies and only the necessary convergence studies; presents before/after evidence and receives Auditor review. | PENDING | Instrumentation qualification is accepted from default bitwise identity plus an isolated clean qtop=2 high-frequency production solve on the approved N=2520 baseline (active N=2752). The scientific five-frequency q sweep has not run and remains required; this is no longer a production-patch blocker. |
| CH-05 | Test validity is audited: correct target/call path/config, assertions sensitive to defect, no cache contamination, intended geometry actually changed, and no circular oracle. | PASS | The gate now targets the correct Fine grid, uses final active geometry, records real changed hashes/counts/cache key, rejects deliberately coarse rounding and a folded wall, validates production gamma/column assembly, and demonstrates a genuine isolated MISS. `29/29` is supported by inspected sensitive contracts; it does not waive the separate OFAT-identifiability restriction. |

## 9. Deliverables and decision questions

| ID | Requirement / acceptance evidence | Status | Evidence / note |
|---|---|---|---|
| DL-01 | Deliver `PHASE2_2_OUTER_DOMAIN_AUDIT.md` with all 18 requested sections, including this requirement checklist and an independent Auditor Review. | PENDING | |
| DL-02 | Retain supporting CSV, MAT, convergence figures, BC residual diagnostics, damping-closure comparison, and before/after comparison; report links/paths resolve. | PENDING | |
| DL-03 | Report directly answers Q1 root cause of ~96% high-frequency B33 outer sensitivity and Q2 apportions radius/resolution/radiation BC/FS BC/extraction/flux/conditioning or mixed causes with data. | PENDING | |
| DL-04 | Report answers Q3 first failing damping path, Q4 whether F3 shares B33 root cause, Q5 whether Fine outer is asymptotically converged, and Q6 whether ready for multi-body. | PENDING | |
| DL-05 | Executor prints exactly the nine requested final state variables with `YES/NO/PARTIAL/NOT_APPLICABLE` only as allowed by each field. | PENDING | |
| DL-06 | Auditor independently prints: `REQUIREMENTS_COMPLETE`, `PHYSICS_CHECK`, `NUMERICAL_VALIDATION`, `CONVERGENCE_CLAIM_JUSTIFIED`, `FORBIDDEN_SHORTCUTS_USED`, `UNRESOLVED_BLOCKERS`, `FINAL_AUDIT`. | PENDING | |
| DL-07 | Final baseline is reproducible, explainable, and demonstrates genuine numerical convergence where claimed; no premature multi-body transition. | PENDING | |

## 10. Mandatory checkpoint protocol

### Checkpoint 1 — Executor plan

Evidence requested: a concise plan naming baseline frequencies, clean-recompute controls, nine-factor OFAT matrix, radius×resolution design, BC derivation/damping-closure workflow, raw diagnostics, expected artifacts, change gate, and review handoffs.

Decision rule: `PASS` only if the plan covers the checklist without prohibited scope or premature production edits. Gaps that can be corrected before execution are `BLOCKER`, not deferred silently.

**Current verdict:** `PASS`.

Plan coverage is complete enough to proceed. No production change or numerical conclusion is authorized by this verdict. Execution evidence remains pending in the tables above.

Carry-forward warnings (must be resolved at the named later checkpoints):

1. `IPOTEN=1` is not by itself proof of a clean recompute; Checkpoint 2 requires observed cache MISS, isolated outputs, and keys/hashes tied to every varied geometry/BC parameter.
2. The old `PARA9` coupling, frequency-dependent automatic outer expansion, and dynamic `mu` can silently violate OFAT. Checkpoint 2 requires an explicit invariant/delta table of the *effective* per-frequency parameters, not only input configs.
3. Exact radius/resolution levels and the tolerance used to call `h/lambda` constant are deferred to Checkpoint 2.
4. Component BC residuals must not merely partition the solved matrix residual. Checkpoint 3 requires a definition showing what independent physical defect they can detect; otherwise they must be labelled algebraic residuals only.
5. Existing “energy” output is not an independent flux closure. `B_flux` must be newly derived/evaluated from actual energy flux, while Haskind remains separate or `NOT VALIDATED`.
6. A condition estimate must state matrix scaling and estimator limitations; if it is not reliable, report unavailability/proxies rather than a spurious precise number.

### Checkpoint 2 — Outer-domain design

Audit OFAT isolation, actual active-mesh change, full per-run provenance, wavelength normalization, three orthogonal groups, and clean-cache discipline.

**Current verdict:** `BLOCKER — Core-A radius isolation passes, but P-full is required before the official OFAT/orthogonal sweeps`.

Independent findings:

- Fine body 588, frequencies `{0.5,1.1,1.5,1.7,2.0}`, isolated clean paths, effective manifests, complete geometry/operator/cache hashes and observed `MISS` remain the mandatory official-run contract. Automatic expansion and dynamic `mu` must remain frozen except when the named radiation-parameter factor is itself under test.
- Core-A is a genuine held-resolution common-radius design. Its production-geometry preflight covers 15 states and passes 15/15: `q=(R-20)/lambda`, three levels add 0/1/2 legacy radial layers, preserve one body hash per frequency, produce three distinct FS/bottom/outer/merged hashes, keep all audited directional spacings within `+/-5%`, and have no finer-rounding exceptions. `delta R/lambda` spans `0.97–1.82` across the five frequencies and total unknowns range from 2520 to at most 3352.
- Core-A is only a radius-sensitivity screen. The held starting mesh is itself strongly wavelength dependent: the outer-wall vertical maximum grows from about `0.0569 lambda` at `omega=0.5` to `0.8115 lambda` at `omega=2.0` (about 1.23 panels/wavelength). Three radius levels and roughly one high-frequency wavelength of span do not prove a plateau or asymptotic convergence. If the BEM response is non-plateauing or oscillatory, add a farther noncommensurate radius level or report `NOT_CONVERGED`; do not select a visually preferred level.
- The present controls cannot independently refine the fixed 56-node azimuthal interface. Therefore Core-B and Core-C are only radial-plus-vertical studies, not the requested all-direction `h/lambda` experiments. A common `DOMAIN_AZIMUTHAL_COUNT` (P-min) would repair B/C tangential control but would still retessellate the nominally fixed endpoint in top-only/bottom-only extent studies.
- P-full, or a mathematically equivalent endpoint-independent design, is required to satisfy explicit OFAT factors 2 and 4. The minimal acceptable resolver may use a common azimuthal default plus optional independent FS-outer and bottom-outer counts; it need not duplicate three unrelated implementations. Unequal endpoint counts require a deterministic watertight wall zipper. Until this passes, existing top/bottom frustum rows remain supplemental `truncation-shape sensitivity`, not pure FS/bottom radial-extent OFAT.
- Factor 7 must use a matched baseline. If sponge-start levels fix `width=0.75 lambda` while the global baseline is `1.5 lambda`, either restore `1.5 lambda` for factor 7 or include the same-start `0.75 lambda` bridge shared with factor 8. Start-study deltas must never be compared directly against the global case across a simultaneous width change.
- After P-full, Core-B may vary a declared composite resolution vector at fixed radii/start/width/mu, provided FS radial/tangential, bottom radial/tangential and wall vertical/tangential actual `h/lambda` all change monotonically, are sufficiently separated and are not rounded away. Core-C must pair that measured resolution vector with the radius levels and remains mixed-error evidence only. Individual factors 1 and 3 must be named `FS_RADIAL_PANEL_SPACING` and `BOTTOM_RADIAL_PANEL_SPACING` unless separate tangential sub-audits are also executed.
- The 5500-unknown limit is a safety cap, not evidence that a dense solve is affordable. Before formal sweeps: preflight every planned geometry; pass the P-full topology/assembly/cache CP4 gate; run one clean maximum-N active solve; then pilot high-frequency extremes (`omega=2.0`, followed by 1.5/1.7) before mid/low points. Record runtime, memory and artifact size. If the required matrix cannot be completed, report a resource `BLOCKER` rather than silently dropping factors or frequencies.

P-full implementation/preflight is authorized, but no formal BEM sweep is authorized yet. Its CP4 gate must demonstrate:

1. Absent versus explicit default azimuthal controls are bitwise identical in geometry, boundary operator, results and cache semantics.
2. A common-count change alters only intended FS/bottom/outer tessellation and holds body, radii, sponge start/width/mu and physical formulas fixed.
3. A top override preserves the bottom mesh hash/count/coordinates bitwise; a bottom override symmetrically preserves the top; independent-input delta is one and all wall/count/cache changes are whitelisted closure consequences.
4. Unequal-count zipper meshes pass shared-ring/watertightness, edge multiplicity, Euler/connectivity, winding/normal, positive area/Jacobian, no-self-intersection and independent panel-integral controls, plus the active `D+gamma*S` assembly oracle.
5. A clean active one-frequency solve observes cache `MISS`; no-op, rounded-away and two-input co-variation negative controls fail as intended. Every level records bandwise radial/arc/chord/wall `h/lambda`, counts, component/merged/source/collocation/operator hashes and the new code fingerprint.

Fingerprint `78aff7cdc...` is frozen only for the pre-P-full solver state. Because P-full changes mesh/cache dependencies, its accepted CP4 submission must produce and freeze a new fingerprint and a new clean baseline; evidence from the two fingerprints must not be spliced into one sweep.

#### P-full atomic implementation-plan review

**Decision:** `PASS — Executor may begin the mesh/control instrumentation patch; formal sweeps and scientific attribution remain blocked`.

The proposed patch is within the authorized single-issue scope: optional common/top/bottom azimuthal controls, endpoint-independent horizontal remeshing, an unequal-count watertight wall zipper, effective manifests/cache/fingerprint binding and their topology/integration/active-assembly tests. Only Executor may edit production files. This authorization does not permit a change to Green kernels, the FS Robin or local outer ABC formula, radiation/diffraction RHS, pressure/traction, sponge physics, damping closures, RAO, projection/clipping or WAMIT data.

Hard interpretation and evidence conditions:

1. In official factor 2, `q_top` must be the **only changed independent input**. The top endpoint count must be resolved internally from `q_top` and a frozen tangential-spacing rule; it must not be supplied as a second independently varied override. The bottom count/radius/coordinates/hash must be bitwise invariant. Factor 4 is the exact mirror. Resolved counts belong in the derived-closure whitelist, while the canonical independent-input delta remains one.
2. The three optional controls may share one resolver with a common default and endpoint overrides. Missing controls and explicit legacy/default values must take the old equal-count path and be bitwise identical. A common-count B-level is a declared composite resolution setting, not another physical-radius factor. Invalid/noninteger/small counts, rounded-away scientific levels and unintended simultaneous endpoint overrides must hard fail.
3. A triangular zipper represented through a degenerate quadrilateral is acceptable only if every active panel has positive physical area, finite center/normal/source data and a valid production quadrature/Jacobian. Duplicate vertices must not create zero-length integration maps or corrupt the matrix self term. Independent off-surface reference and an actual assembled unequal-count domain are mandatory; topology flags alone are insufficient.
4. Top-only/bottom-only tests must compare exact coordinates, ordering, counts and hashes of the fixed horizontal component, not merely equal radii or equal aggregate panel counts. The changed wall is a closure consequence and must retain analytic normal/winding/area/volume, no-T-junction, connectivity/Euler and self-intersection negative controls.
5. The active unequal-domain gate must verify the existing `D+gamma_j*S` column against the production helper without copying its logic into the oracle, persist endpoint counts/targets/actual bandwise metrics and all geometry/operator/cache hashes, and observe a clean `MISS`. Default value neutrality must cover geometry and operator/results semantics. These tests validate execution of the unchanged ABC; they do not validate an exact DtN or physical convergence.
6. Exact Core-B/Core-C levels are not approved merely by adding controls. Geometry preflight must show monotone, sufficiently separated FS radial/tangential, bottom radial/tangential and wall vertical/tangential `h/lambda`, unchanged radii for B, stated paired radii for C, unique hashes/cache keys and `N<=5500`. Any rounded-away or unaffordable level is a design `BLOCKER`, not permission to omit a frequency.
7. The factor-7/factor-8 bridge is accepted: factor 7 compares start offsets at fixed `width=0.75 lambda`; its `start=20,width=0.75 lambda` state is exactly shared with factor 8, whose `width=1.5 lambda` state reconnects the global baseline. The shared cases must have identical config/effective/operator/cache hashes and results. Report factor-7 sensitivity only within the matched `0.75 lambda` block; do not compare its other start levels directly with the global `1.5 lambda` baseline.
8. The implementation sequence is approved as: focused patch -> unit/checkcode/geometry and active assembly evidence -> Auditor CP4 -> maximum-N clean pilot -> high-frequency pilots. The maximum-N and `omega={2.0,1.5,1.7}` runs are resource/production-path pilots, not official sweep results, unless a later exact manifest receives approval. A post-patch fingerprint and clean baseline are mandatory.

#### P-full unequal-wall active-path finding

**Decision:** `BLOCKER for the unequal-count outer-wall path; PASS to replace it with horizontal endpoint adapters under the gates below`.

The stop is valid and is not a failed implementation detail to bypass. The conforming `68 -> 56` wall contains 348 zipper triangles and has `max(abs(n_theta))=0.0163591`; its stored normals remain geometrically consistent (`min winding/analytic-normal dot=0.999866`). The existing local outer ABC was derived and qualified only for an axisymmetric radial/vertical normal and correctly rejects this new tangential component. Suppressing `n_theta`, weakening the assertion, or applying the radial/vertical formula unchanged would mix an unreviewed physical approximation into a mesh-control patch. No unequal-wall assembly, B/C preflight or sweep may proceed on that route.

Do not authorize a general-normal ABC as part of P-full. For a non-axisymmetric boundary normal the normal derivative contains `(n_theta/r) partial_theta Phi`; it cannot in general be reconstructed from the current scalar radial/vertical local closure without a modal/nonlocal or explicitly approximate tangential operator. Such a change would require a separate mathematical derivation, manufactured azimuthal-mode tests, fail-before evidence and a new one-issue CP4 authorization.

The preferred geometry-only replacement is feasible: keep a common fixed `N_wall` and angular alignment on every outer-wall ring, retain the existing structured frustum quads with `n_theta` at the qualified axisymmetric tolerance, and connect an independently resolved FS or bottom outer ring to the wall ring through a finite-width horizontal zipper adapter carrying the same horizontal-surface BC. This remains within Executor's authorized mesh/control scope only if all of the following hold:

1. The adapter has strictly positive radial width. Two different-count rings at the same radius are forbidden because they create a zero-area annulus. Its inner ring is exactly shared with the bulk horizontal mesh and its outer ring is exactly the structured-wall ring; there are no duplicate boundary sheets or unmatched/T-junction edges.
2. Top adapters are planar at `z=0` and receive the canonical FS source orientation and Robin coefficient; bottom adapters are planar at `z=-h` and receive the canonical bottom Neumann treatment. No adapter panel may be classified as FARFIELD or receive `gamma`. The structured wall alone receives the unchanged `D+gamma*S` operator.
3. For factor 2, only `q_top` changes independently. The bottom horizontal mesh, bottom adapter (if present), its coordinates/order/count/hash and `N_wall` are bitwise invariant. Factor 4 is symmetric. Wall radius/coordinates/normals/area/hash and the changed-side adapter are derived closure consequences. The independent-input delta must remain one.
4. `N_wall` is frozen across all levels of a given extent block. Because its actual tangential `h/lambda` changes with radius, it must be chosen by a declared worst-radius resolution rule and preflighted at every level. If this uses a locally finer count than the global baseline, include the same-radius/count bridge and do not compare across unmatched counts. A fixed but grossly under-resolved `N_wall=56` cannot support an extent-isolation or convergence claim merely because the count is unchanged.
5. Adapter width, ring-count resolver, angular origin and grading rule are frozen, dimensionally stated and hashed. Record radial/arc/chord `h/lambda`, aspect/skew/Jacobian and rounding provenance per adapter band. Count changes on the varied side must be internally derived from the one radius input under that fixed rule, never entered as a second scientific control.
6. Horizontal zipper triangles represented as degenerate quads must pass positive-area/finite-center/finite-normal checks, production panel quadrature and self-term behavior, independent off-surface potential/normal-reference tests, canonical FS/bottom double-layer orientation, and wrong-orientation/zero-width negative controls.
7. The closed domain must again pass shared-edge multiplicity, Euler/connectivity, ring equality, winding/analytic normals, area/volume/divergence, no-self-intersection and active production assembly. The active oracle must show horizontal adapter columns use the correct FS Robin or bottom Neumann operator, wall columns use the unchanged axisymmetric `D+gamma*S`, and `max(abs(n_theta_wall))` satisfies the existing helper tolerance without loosening it.
8. Missing/explicit-default controls and equal-count cases must retain the legacy structured path bitwise. Cache/effective/operator manifests must distinguish adapter policy/counts/hashes and force old potential/result caches to `MISS`. Submit the focused revised diff and gates for CP4 before any maximum-N solve or pilot.

### Checkpoint 3 — BC mathematics

Independently check time convention, `iω`, normals, radiation/diffraction/FS/bottom/outer BCs, pressure and A/B extraction, excitation, amplitude and finite-depth normalization.

**Current verdict:** `PASS — the cylindrical and active-frustum BC/normal/assembly chains are qualified; scientific radius-only attribution remains an Outer-domain experimental-design blocker, not a BC blocker`.

Independent derivation and findings:

1. With `G=1/r` and fluid-domain outward normal `n_Omega`, Green's boundary equation is
   `(1/2 I + K_Omega) Phi = S q_Omega`.
   If every stored/source normal is `n_s=-n_Omega`, then `K_s=-K_Omega`, `q_s=-q_Omega`, and the equivalent equation is
   `(1/2 I-K_s) Phi = -S q_s`.
   The code's `D_s=1/2-K_s` therefore does **not** permit arbitrary mixed source orientations.
2. The body, bottom and cylindrical outer source normals are inward to the fluid domain (`-n_Omega`); FS `+e_z` is outward from the fluid (`+n_Omega`). Thus only the FS violates the matrix's assumed source orientation. Because `rankine_panel_integrals` obtains the solid-angle sign from vertex order, changing only the stored normal is insufficient; supplied normal, winding and analytic derivative must agree.
3. Radiation for unit generalized displacement has body/source derivative `q_R,s=iω n_j`. The correct RHS is `-S iω n_j`; the current `+S iω n_j` is reversed.
4. Fixed-body diffraction has `q_D,s=-q_I,s`; substitution gives `rhs=+S q_I,s`, so the current diffraction body RHS is correct under the stated body normal.
5. On the physical FS, `partial_z Phi=(ω²/g)Phi=k tanh(kh)Phi`. The real part of the current `nu=k` is not the finite-depth coefficient except asymptotically. A sponge may make the Robin coefficient complex, but its exact PDE and sign must be stated; it cannot remove the physical-region factor `tanh(kh)`.
6. Bottom `q_s=0` with stored `+e_z=-n_Omega` is consistent once the global source convention is enforced.
7. For `exp(+iωt)`, a cylindrical outgoing mode is `Phi~f_kappa(z) exp(-i kappa r)/sqrt(r)`. With inward source normal, `q_s/Phi=i kappa+1/(2r)`. The correct BIE substitution is `D_s+S(q_s/Phi)=0`; production code uses the opposite S sign and omits `1/(2r)`.
8. For `R(z)=R_b+R'(z+h)`, inward `n_s=(-e_r+R'e_z)/sqrt(1+R'^2)`, so
   `q_s/Phi=[i kappa+1/(2r)+R' kappa tanh(kappa(z+h))]/sqrt(1+R'^2)`.
   This formula is correct only when `kappa` is the horizontal (generally complex) root consistent with the local FS Robin coefficient and bottom condition. In a sponge plateau, substituting the undamped real physical `k` is inconsistent.
9. Linear Bernoulli pressure is `p=-iρ ω Phi`. Force on the body is `Q=-integral(p n_B dS)=+iρ ω integral(Phi n_B dS)`. Production radiation and excitation integrations use the opposite body-force sign. The radiation RHS and force errors can double-cancel in A/B, so preserving today's positive-looking coefficients is not evidence either is correct.
10. With physical `Q_R=(ω²A-iωB)xi`, extraction is `A=Re(Q_R)/(ω²xi)` and `B=-Im(Q_R)/(ω xi)`. The RAO matrix `-ω²(M+A)+iωB+C` is correct for `exp(+iωt)` **if** A, B and excitation are physical body-force quantities. Current excitation sign gives a 180-degree phase error even where amplitude is unchanged.
11. Direct flux must use a pre-sponge outward control normal. For diagonal/unit displacement,
    `P_out=1/2 Re integral(p q_out* dS)=1/2 ω²B`, hence
    `B=(ρ/ω) Im integral(Phi q_out* dS)`.
    For cross modes, build the correctly indexed Hermitian pressure-work form and retain its raw non-Hermitian/non-real residual before reporting a real symmetric B. Do not PSD-clip it.
12. Existing “energy” is Haskind-derived, not flux. The current Haskind plus-sign integrand is not the antisymmetric Green concomitant under one normal convention and remains `NOT VALIDATED` pending a manufactured identity.

Mandatory minimal-test/fix order:

1. **T1 time/amplitude** — incident phase propagation, dynamic/kinematic FS identities, peak amplitude and pressure. This must pass without solver fitting.
2. **T2 Green/source orientation** — constant harmonic field on a simple watertight closed boundary using inward source normals; verify `D_s 1=0`, normal reversal, winding reversal and analytic off-surface derivative. This gates all matrix changes.
3. **T3/T4 physical FS and bottom** — finite-depth plane mode must satisfy `partial_z Phi=(ω²/g)Phi` at z=0 and zero bottom derivative; separately test the matrix substitution signs.
4. **T5 cylinder first, then frustum** — manufactured Hankel/finite-depth mode tests `ikappa`, `1/(2r)`, BIE S sign and the vertical projection. Include a complex-kappa sponge case derived from its Robin coefficient. This gates factors 2/4 and all outer studies.
5. **T6 unbounded translating sphere radiation** — exact dipole potential checks body `q_s=iωn_j` and the negative radiation RHS without FS/outer complications.
6. **T10 pressure traction, then T7 coefficient extractor** — analytic panel traction establishes `Q=-integral p n_B`; exact translating-sphere added mass verifies the coordinated RHS/force/A/B convention. Check before/after signs, not magnitudes only.
7. **T8 sphere diffraction** — manufactured/analytic normal cancellation verifies `q_D=-q_I` and excitation body-force sign.
8. **T9 direct flux** — a finite-depth progressive plane wave must reproduce `E c_g` with peak-amplitude factor 1/2; then test radius/quadrature convergence and cross-mode raw/Hermitian bookkeeping.
9. **T11 Haskind last** — only after pressure and flux pass; use a manufactured Green identity to determine normal, sign, conjugation, incident direction and prefactor. Until then report `B_haskind: NOT VALIDATED`.

Current resolution status after the atomic fixes:

- T1–T7/T10 now pass for the current cylindrical production baseline. The former radiation-RHS/traction double cancellation is removed: the corrected radiation potential and corrected load individually satisfy their analytic oracles. Cylinder-based OFAT factors may proceed subject to their own Checkpoint 2 authenticity gates.
- T8 now exercises the production diffraction sign and excitation chain with separate analytic and active-path controls. The Fine full-sphere current solution/analytic-row errors are `0.559%/0.555%`, consistent with panel discretization and far below the opposite-sign approximately `199.45%` errors. The production excitation amplitude error is `0.275%`, matching the independent surface-quadrature scale, with zero phase error. Thus the former T8 gate is released without a production edit.
- OFAT factors 2/4 remain blocked until the proposed watertight active frustum is generated and the production outer helper/normal projection passes on that closed solver geometry. The existing open-side actual-winding test proves the local formula but not active-domain closure/tessellation.
- Direct flux and Haskind do **not** block the BC-mathematics checkpoint. They remain independent damping-closure blockers under DP-02/DP-03/DP-05/DP-07 and will block final Phase validation if absent or substituted.

#### T8 review — diffraction/excitation sign and normalization

**Decision:** `PASS — no production change is justified`.

- The manufactured uniform-flow sphere is a valid Laplace/diffraction sign oracle independent of free-surface and outer-boundary approximations. On the mirrored Fine mesh (1176 panels), the production assembler/solve with `+S*q_I` differs from the analytic dipole by `0.5594%`, and the analytic field's assembled-row residual is `0.5550%`. These are coherent panel-discretization errors; the opposite RHS gives about `199.45%` residual/solution error and the numerical opposite solution is exactly the negative of the current one.
- A 5% analytic BEM tolerance is broad relative to the observed 0.56% but acceptable for a **sign** gate: it was not selected using WAMIT, independent negative controls are separated by roughly two orders of magnitude, the solved linear residual is `1.66e-15`, exact `q_D+q_I` is `4.21e-16`, and the off-surface finite-difference derivative error is `1.30e-10` against a fixed `1e-8` limit. It is not a convergence tolerance or accuracy claim.
- The production excitation routine receives the analytic incident-plus-dipole total potential. The force amplitude differs from `2*pi*i*rho*omega*U*a^3` by `0.2746%`, consistent with the independent Fine-sphere quadrature error, and phase error is zero. The old traction sign differs by `199.7%`. This segmented manufactured chain isolates RHS and force signs; it should not be described as a full outer-domain accuracy validation.
- Independent finite-depth incident-potential and normal-velocity formulas close to machine precision; the fixed-body cancellation and peak free-surface amplitude/phase also close to machine precision.
- The active run is not a grep-only check. With `IPOTEN=1, IFORCE=0, IRAD=0, IDIFF=1`, profiler data show `run_frequency_domain_case.m` execute the exact `rhs(:,headingIndex)=S*dphi_dn` line once. The log records `forced_recompute`, body/total counts `588/1848`, a nonzero diffraction potential, exact stored incident-potential reproduction, code fingerprint `87d68ee...`, and diffraction linear residual `6.72e-14`.
- All 11 state rows match, including required failures for the opposite RHS and old traction; the relevant production/test files pass `checkcode`. No solver/cache/config/force code was modified for T8.

#### Authorization — active watertight frustum and diagnostic preparation

**Frustum production instrumentation:** `PASS to implement, not yet PASS to use for scientific attribution`.

The authorized scope is the Checkpoint 2 geometry/manifest design only: independent top/bottom physical radii; deterministic closure-derived wall vertices, counts and normals; default `R_t=R_b` cylinder bit-identical regression; watertight shared rings, connectivity/Euler, winding, area/volume, no-self-intersection and spacing assertions; complete component/config/cache hashes; and actual production-helper preview/assembly equality on the closed frustum. Do not change the outer-ABC formula, FS/sponge physics, RHS, forces, damping closure or RAO. The diff, default-cylinder regression and closed-frustum source-column/normal tests must return to Auditor before factors 2/4 execute.

**Cylinder OFAT/diagnostic work:** `PASS to begin in parallel under the approved design`.

- Factors 1, 3 and 5–9, plus cylinder-compatible diagnostic instrumentation, do not depend on the frustum and may proceed with Fine body fixed, per-factor clean MISS, one-independent-input assertions, real active hashes/counts/matrix metadata and directional `h/lambda` reporting.
- Diagnostic-only schema/instrumentation changes must not alter matrix/RHS/solution values and still require a focused diff/regression review if they touch production files. Full-boundary potentials/residuals must be labelled algebraic versus independent physical residuals exactly as previously required.
- Do not mix runs from different code fingerprints into one convergence sequence. Freeze the diagnostic-enabled executable first; if instrumentation changes after a run, rerun the affected baseline/levels cleanly. Frustum factors 2/4 remain excluded until their separate blocker clears.

### Checkpoint 4 — Production-code modification

Review the focused diff plus pre-change baseline, theory, minimal reproduction/test, clean representative reruns, convergence impact, and absence of forbidden shortcuts. Insufficient evidence is a `BLOCKER`.

**Current verdict:** `PASS — PARA13/operator/raw-diagnostics remediation clears the instrumentation blockers. Fingerprint 9470060d... may be frozen for the narrowly authorized cylinder factors below; pure-radius, full tangential-resolution and damping-closure claims remain outside this authorization`.

#### CP4 pre-change authorization 1 — double-layer source orientation

**Decision:** `PASS — authorization limited to this one issue`.

Accepted fail-before evidence:

- All 19 rows match their declared before-state, while physical `currentPass` remains visibly 10/19; expected failures are not counted as validation.
- T2 uses the actual `rankine_panel_integrals` kernel. The all-inward closed cube satisfies `D_s 1=0` to `5.55e-17`; changing only the top/FS to current outward orientation produces residual `0.21795`. Both winding-alignment controls pass, isolating a mixed domain/source orientation defect.
- T3/T5/T6/T10/T7 independently remain red with the expected signs/magnitudes. The complex-kappa row is honestly `NOT_IMPLEMENTED`.

Authorized implementation contract:

1. Canonical double-layer/source orientation is `n_D=-n_Omega` for every component. Mapping is body `+1`, FS `-1`, bottom `+1`, outer `+1` relative to the stored/winding derivative.
2. Apply the component sign to each source-column `dGdn` contribution (after image summation is equivalent) before forming `Dij=-dGdn/(4pi)`. It must affect **all collocation rows** for that source column.
3. Do not multiply `Sij`, prescribed Neumann RHS, stored physical normals, pressure normals, or the `+0.5` boundary self coefficient. The self coefficient is added once and is orientation-independent after canonicalization.
4. Assert stored normal–winding alignment per component before using the fixed component map. A silent inconsistent BMF must fail rather than receive the wrong canonical sign.
5. Store the named convention and component signs in `assembly_info` and the effective/cache manifest. The `assemble_rankine_matrix.m` file hash already invalidates potential cache through `get_rankine_code_version`; any new shared production helper must also be added to that version fingerprint.
6. Preserve fail-before CSV/MAT/MD unchanged and create separate after evidence. T2 mixed case must turn green near machine precision; T3/T5/T6/T10/T7 must remain in their prior physical fail state, showing this patch did not smuggle other convention fixes.
7. The after-test must touch the production canonicalization path. A hard-coded second copy of the orientation multiplication is not sufficient by itself. Diff inspection plus production-reported signs/hash are mandatory.
8. No WAMIT comparison or outer-domain solve is evidence for this patch. No reciprocity projection, PSD clipping, scaling, smoothing or unrelated modification is authorized.

#### CP4 post-change review 1 — commit `938fb91`

**Code/unit decision:** `PASS`.

- `get_rankine_source_orientation` uses `cross(v3-v1,v4-v2)`, which equals the sum-direction of the two triangle area vectors used by the solid-angle winding for a planar quadrilateral. It validates alignment against stored normals and rejects ambiguity.
- `merge_domain_geometry` orders body → FS → seabed → farfield, matching the helper's count slices. Empty components advance correctly.
- `assemble_rankine_matrix` applies one component sign to the complete image-summed derivative for source column `j`, before `Dij=-Dsum/(4pi)`, while S and the separately added self `0.5` are unchanged. Symmetry reflection plus the existing vertex swap preserves stored-winding alignment, so the original component relation to the fluid domain is unchanged and no second sign flip is needed.
- `assembly_info` exposes convention/sign/hash. Both base and per-frequency cache manifests bind them. `get_rankine_code_version` fingerprints both the assembly and the new helper, so helper-content changes invalidate the potential cache.
- Separate after evidence reports 13/13 contract rows: T2 changes from `0.21795` to `5.55e-17`; T3/T5/T6/T10/T7 retain exactly their prior failing metrics. `checkcode` is reported clean.
- The canonical-D after oracle calls the production helper but reconstructs the final D multiplication locally; it is not a fully black-box D-operator test. A real production assembly and cache-key invocation verify that the helper contract reaches those paths, and direct diff inspection verifies the multiplication. This is acceptable for this small patch but should not be generalized to larger fixes.

**Clean numerical decision:** `PASS`.

- `Orientation_Clean_Comparison` retains five exact pre/post pairs at omega `{0.5,1.1,1.5,1.7,2.0}`. All ten output/cache/mesh paths were absent at start, used `IPOTEN=1`, and report both `cacheStatus=miss` and log reason `forced_recompute`.
- Every pair has the same physical/numerical configuration, Fine body count 588, component counts `560/1148/224`, total unknowns and matrix size 2520, component/merged/source/collocation hashes, wavenumber, wavelength, effective mu, sponge width and assembly group count. Pre/post code fingerprints and per-frequency cache keys differ.
- The isolated shadow preserves the active pre-existing Phase 2.1 work and reverts only the five orientation files. The retained construction patch changes exactly the new helper and its four dependent production files; the snapshot SHA table agrees. The snapshot inventory is limited to those five files rather than every executable dependency, but stable per-side code fingerprints and the exact construction patch are sufficient for this focused change. Future shadow comparisons should persist the full code-version file table where available.
- Raw 6x6 A/B, complex excitation and RAO including phase, raw reciprocity, radiation/diffraction linear residuals and body-potential norms are persisted in CSV/MAT. Schema-5 did not retain full FS/bottom/outer potentials; this limitation is explicit and does not invalidate attribution of this atomic patch, but it does not satisfy the later full-boundary diagnostic requirement RD-02.
- The numerical effect is large: across the high-frequency points, reported relative changes reach about 35.0% in A33, 75.2% in B33, 66.9% in `|F3|`, and 75.2% in `|RAO3|`. This is correctly described only as impact, not correctness, root-cause closure or convergence. T3/T5/T6/T10/T7 remain red.
- A separate convergence study is not informative at this intermediate state because several independently demonstrated operator/traction defects remain. Deferral is accepted only because no convergence claim is made; final required convergence remains blocked until the convention defects are corrected and the OFAT/orthogonal studies execute.

No forbidden scaling, WAMIT tuning, smoothing, projection, clipping or plotting substitution appears in this change or its evidence.

#### CP4 pre-change authorization 2 — physical finite-depth free-surface Robin scale

**Decision:** `PASS — authorization limited to the exact Robin base-scale defect below`.

Fail-before evidence is sufficient: T3 gives current `k=0.0285926065 1/m` versus the exact physical coefficient `omega^2/g=0.0254929053 1/m`, a relative error of about 12.159% for that case. Under `exp(+i omega t)`, the finite-depth progressive mode obeys
`partial_z Phi = k tanh(kh) Phi = (omega^2/g) Phi`.

Authorized implementation contract:

1. Introduce a named physical Robin base `nu0=omega^2/g` and use `nu_FS(r)=nu0*(1-i*mu(r))`. This replaces only the present base factor `k0` in the FS Robin coefficient. `k0` remains the finite-depth horizontal dispersion root for wavelength and other still-unfixed formulas.
2. Do not change the damping profile, dynamic/effective-mu rule, sponge start/width, mesh, outer-boundary coefficient/sign/decay, radiation RHS, force/traction extraction, diffraction RHS, RAO, or any raw/projected quantity in this patch. Retaining those known defects is not validation of them.
3. Expose `nu0`, the complex-Robin convention and enough effective coefficient statistics to verify the active production path; bind them through the effective/cache audit. The changed assembly file already changes the code fingerprint, but the actual coefficient must be inspectable rather than inferred only from a source diff.
4. A production-path after test must make T3 pass near floating-point tolerance while T1/T2/T4 stay green and T5/T6/T10/T7 retain their declared unresolved states. Do not accept a formula-only duplicate as the only oracle.
5. Reuse the current clean post-orientation runs as the pre-change baseline only if their code fingerprint exactly matches the state immediately before this patch. Then run the same five isolated frequencies after the patch with actual forced cache MISS and the same geometry/configuration/hash contract; retain the same raw before/after quantities and make no correctness/convergence claim.
6. No outer-domain/OFAT inference is authorized from this intermediate patch. The outer complex-kappa relation and outgoing BC remain separate T5 work and the Phase experiments remain blocked by Checkpoint 3.

#### CP4 post-change review 2 — physical finite-depth FS Robin

**Decision:** `PASS`.

- The exact production construction patch has SHA-256 `24783ced41fe9feaa80a576c5775305ec4bad2ec2e8db6d856a7a577e87eae08` and contains only three production files. `assemble_rankine_matrix` introduces `nu0=omega^2/g`, changes only FS `nu=k0(1-i mu)` to `nu0(1-i mu)`, and leaves outer `nu(ff_idx)=k0`; the other two files bind the named/numeric convention in base and per-frequency cache manifests.
- The focused test calls the real assembler. Its physical-region FS source column has zero relative error against `D-(omega^2/g)S` and 0.115739 separation from the old `D-kS` column. T3 and its assembly/cache contracts pass; T1/T2/T4 remain green and T5/T6/T10/T7 retain their exact unresolved metrics. All 18 rows match the declared after-state; no failed row is relabelled as validated.
- The five after runs are isolated `IPOTEN=1` forced cache misses. The reused five-point before baseline is accepted because every run retains its original clean MISS/no-preexisting record, its fingerprint is exactly `ea4715...`, and the saved 19-file code-version dependency list/SHA is stable across all before points. The after fingerprint is `2d667...`; the dependency comparison changes only the assembly and local base-cache implementation among those 19 entries. The separately versioned standalone cache builder is captured by the three-file construction patch and demonstrably changes the entry cache specification/key.
- All five comparison contracts pass: Fine body 588; FS/bottom/outer `560/1148/224`; matrix 2520 square; identical configuration, component/merged/source/collocation hashes, wavenumber/wavelength/effective-mu/sponge values; distinct code fingerprints/cache keys. Raw matrices, complex phases, reciprocity/linear residuals and body potential norms are retained.
- The low-frequency numerical impact is large (at omega 0.5, relative A33/B33/`|F3|` changes are approximately 72.2%/83.3%/10.1%), while every omega >=1.5 effect is <= about `1.8e-9`. Therefore this defect is real but is not an explanation for the reported high-frequency outer-domain sensitivity. The evidence makes exactly that limited inference and no convergence claim.
- Full FS/bottom/outer potential vectors remain unavailable in the old cache schema. This is an explicit limitation and keeps RD-02 pending; it is not hidden or replaced with fabricated data.

No forbidden empirical correction, scaling, smoothing, clipping, projection or WAMIT-based tuning was used.

#### Next change gate — T5 outer outgoing condition

**Production authorization:** `PASS — limited to a first-order local asymptotic absorbing closure; it must not be described as an exact finite-radius DtN map`.

Accepted fail-before evidence:

- Twenty branch-continuation cases cover the five representative frequencies and `mu/(4/pi)={0.5,1,1.5,current}`. Starting at the positive physical real root, every continuation retains `Re(kappa)>0`, `Im(kappa)<=0`, converges in at most five Newton iterations per step, and has normalized dispersion residual <=`3.47e-16`. No sign/absolute-value projection is used to force the branch.
- Exact manufactured cylinder fields for angular modes 0 and 1 verify `gamma_exact=-kappa H_m^(2)'(kappa R)/H_m^(2)(kappa R)` to <=`3.89e-16`. The local approximation `i*kappa+1/(2R)` is not called exact: its error is tabulated at physical `kR={2,4,8,16,32}` and, on independent Auditor inspection, is monotonically decreasing in all 40 frequency/mu/mode groups.
- Five actual calls to production `assemble_rankine_matrix` reproduce the implemented `D-i*k0*S` outer source column to about `1e-17`, while differing from the candidate outgoing form by `0.409–1.147`; all five candidate rows remain explicit fail-before failures.
- Forty complex-kappa frustum cases use actual quadrilateral vertices/winding. Stored-normal alignment is >=`0.999999995`, the analytic projected-normal identity closes to <=`6.75e-16`, and faceted-normal error is <=`1.081e-4`. The surface is correctly labelled an open side diagnostic, not a watertight solver domain.
- The evidence explicitly identifies the decisive limitation: exact impedance is angular-mode dependent, so a scalar local coefficient cannot be exact for both m=0 and m=1. A modal/nonlocal DtN would be a different, larger method. The authorized choice is the documented first-order local asymptotic condition, not an empirical fit.

Authorized implementation contract:

1. Add one branch-continuous complex-dispersion helper solving
   `kappa*tanh(kappa*h)=nu0*(1-i*mu_outer)` from the positive real physical `k` at `mu_outer=0`. It must enforce by validation, not projection, `Re(kappa)>0`, `Im(kappa)<=tolerance`, a normalized residual tolerance no looser than `1e-11`, bounded Newton iterations and adaptive continuation refinement. Failure must stop assembly; no silent fallback to real `k0` or a different root is allowed.
2. Use the **actual effective plateau value** that reaches the outer FS ring (`mu_outer=effective_mu0` under the current profile), not the nominal input `cfg.fs.mu0`. Record both. If a future start/width geometry does not reach a plateau at the outer ring, the local coefficient must use the actually evaluated outer-ring FS value and demonstrate its consistency; it may not assume the plateau silently.
3. For each outer source panel `j`, define panel-local `r_j=hypot(x_j,y_j)` and `z_j` from its source center and use its actual stored-inward normal. Let `n_r=n_s·e_r`, `n_z=n_s·e_z`. The authorized scalar is
   `gamma_j=(-n_r)*(i*kappa+1/(2*r_j)) + n_z*kappa*tanh(kappa*(z_j+h))`.
   Assert finite positive `r_j`, inward radial orientation (`n_r<0` within a documented geometric tolerance), and negligible tangential-normal component for the generated axisymmetric wall. For a cylinder this reduces exactly to the authorized approximation `i*kappa+1/(2R)`; for the ideal frustum it reduces to `[i*kappa+1/(2r)+R'*kappa*tanh(kappa(z+h))]/sqrt(1+R'^2)`.
4. Replace only each outer **source column** with canonical `D_j+gamma_j*S_j`; the coefficient is source-panel-local and applies to all collocation rows for that column. Do not change double-layer orientation, S itself, the self `0.5`, body/FS/bottom columns, or RHS/force definitions.
5. Name the method everywhere as `first-order-local-asymptotic-absorbing-BC` (or equally explicit wording), never `exact DtN`, `exact radiation BC`, or a convergence result. Persist in `assembly_info` and base/per-frequency cache manifests: time/outgoing convention; dispersion equation; branch/continuation tolerances and iteration data; physical `k`; complex `kappa`; nominal and actual `mu_outer`; radius definition; normal-projection formula; radial-decay order; min/max of `k*r`, `abs(kappa)*r` and complex `gamma`; the m=0/m=1 exact-versus-asymptotic diagnostic error at the actual radius range; and a hash of the per-panel gamma vector/contract. Add every new production helper to the code-version fingerprint.
6. Preserve the present fail-before files. After the patch, rerun the same production-path cylinder/source-column test using the **assembler's actual effective mu**, and require all five columns to match `D+gamma_j*S` at floating-point tolerance while the old `D-i*k0*S` form fails. The continuation test must call the production helper rather than retain a second solver as its only oracle. Re-run the complex m=0/m=1 and actual-winding frustum manufactured tests through the production coefficient helper. T1–T4 must remain green; T6/T10/T7 must remain their declared unresolved failures.
7. Retain a before snapshot/fingerprint, then run omega `{0.5,1.1,1.5,1.7,2.0}` after the patch in isolated paths with actual forced cache MISS and identical Fine body/configuration/geometry hashes. Save raw A/B, complex F/RAO phase, reciprocity, split/linear residuals available at this stage, body-potential norms and the new kappa/gamma diagnostics. Large changes are impact only, not correctness or convergence.
8. Do not alter FS `nu0`, sponge profile or dynamic-mu rule, sponge start/width, mesh radius/resolution, radiation/diffraction body RHS, pressure traction, A/B extraction, RAO, damping closures, WAMIT data, projection or clipping in this patch. The later OFAT current-radiation-parameter study will test the retained sponge model independently.

Factors 2 and 4 remain blocked from scientific attribution until their **watertight active frustum geometry** exists and the after-test demonstrates the same production coefficient/normal contract on it. The present open-side diagnostic validates only the projected formula.

#### CP4 post-change review 3 — first-order local asymptotic outer ABC

**Decision:** `PASS`.

- The exact construction patch has SHA-256 `6d1a52d66a8e53a693721aac38fe31af231c6b0afbaaf6ebd15301c42923c02b` and contains only five production files: one new helper plus assembly, per-frequency cache, code-version and run-manifest integration. It does not alter the FS/sponge generator or dynamic-mu rule, meshes, RHS, force/extraction, damping closure, RAO, or raw/projected separation.
- The Newton implementation uses the analytic derivative `tanh(kappa*h)+kappa*h*sech^2(kappa*h)`. Its convergence check uses the pre-update residual and update size, which is conservative; the returned root is independently re-evaluated against the final target before acceptance. Every unsuccessful derivative/iteration/branch/residual path causes continuation refinement and ultimately a hard stop. There is no real-k fallback or root projection.
- `actualMu` reproduces the coefficient evaluated at the maximum-radius FS **panel center** using the same quadratic profile and `[0,1]` clamp as FS assembly. This is deterministic and the preview and every symmetry-group assembly are required to return an exactly identical manifest. It is not the geometric-edge nominal mu: in the clean cases actual mu is `0.611–0.885` while nominal effective mu is `1.273`. This is accepted as the explicitly defined discrete interface value, but is a material sponge-resolution/profile coupling to retain in OFAT 7–9 and wavelength-normalized diagnostics.
- With stored-inward normal components, production computes `gamma=(-n_r)(i*kappa+1/(2r))+n_z*kappa*tanh(kappa(z+h))`, then assembles only outer source columns as `D+gamma_j*S`. The radial/vertical signs agree with `H_m^(2)` under `exp(+i omega t)`, and gamma is source-local across every collocation row. Radius, inward-radial and tangential-normal assertions hard-fail incompatible geometry.
- Five actual production assembler columns match the new form to `3e-17–1.6e-16`; all five reject the old form with `0.84–1.21` relative error. The production helper passes complex cylinder and actual-normal frustum formula tests. T1–T4 remain green and T6/T10/T7 remain red. The test's independent D/S reconstruction prevents preview/helper self-agreement from being the only oracle.
- Base cache binds the named non-exact policy; the per-frequency key binds the complete effective helper manifest including actual mu, kappa, branch diagnostics, panel-local radius/normal ranges, actual-radius m0/m1 error, gamma ranges and gamma hash. Preview/assembly exact equality is asserted. The new helper is in the code-version fingerprint. The 20-entry before/after dependency union plus exact five-file construction patch closes the two cache/version integration files that are not themselves listed as numerical dependencies.
- Five reused-before/five isolated-after points satisfy all 17 persisted contract fields: forced MISS/no pre-existing artifacts, Fine body 588, component counts `560/1148/224`, matrix 2520 square, identical config/seven geometry-source-collocation hashes and effective k/lambda/nominal-mu/sponge/nu0. Raw matrices, complex F/RAO phases, reciprocity/linear residuals, body norms and kappa/gamma diagnostics are retained.
- At omega `{1.5,1.7,2.0}`, B33 changes by about `{0.533%,0.326%,0.00077%}` and `|F3|` by `{0.464%,0.167%,0.083%}`. Thus the legacy local outer-row formula is a real defect but, on this fixed geometry, is far too small to explain the former approximately 96% high-frequency outer sensitivity. This does not rule out under-resolution, truncation, reflection, sponge discretization or mixed error and is not a convergence claim.
- Full-boundary potentials are still absent from schema-5 and RD-02 remains pending. No quantity is fabricated or substituted.

No forbidden fitting, scaling, smoothing, WAMIT tuning, projection or clipping is used. Factors 2 and 4 remain blocked until the watertight active frustum path passes; this CP4 pass does not release them.

#### CP4 pre-change authorization 4 — T6 radiation Green-RHS sign

**Decision:** `PASS — authorization limited to the radiation RHS sign and its explicit convention metadata`.

For stored body/source normal `n_s=n_B=-n_Omega`, unit generalized displacement `xi_j=1` gives the physical body-normal derivative `q_s=i*omega*n_j`. The canonical Green equation is `D Phi=-S q_s`, so the radiation algebraic RHS must be `-S*(i*omega*n_j)`. Production currently uses the opposite sign. T6 already separates the correct physical Neumann value from the incorrect algebraic RHS with signed ratio `-1` / relative error `2`.

Authorized implementation contract:

1. Change only radiation RHS construction from `+S*(i*omega*n_j)` to `-S*(i*omega*n_j)` for every radiation mode/symmetry group. Do not negate or redefine `n_j`, body normals, `S`, the solved potential after the solve, or diffraction RHS.
2. Expose and bind: time convention `exp(+i*omega*t)`; radiation basis `unit-generalized-displacement`; stored body/source normal `n_B=-n_Omega`; physical data `q_s=i*omega*n_j`; and Green RHS `-S*q_s`. A small shared production RHS helper is permitted if it is the actual call path and is added to the code fingerprint; otherwise the run-case source hash plus base/per-frequency manifest must bind the direct implementation.
3. Preserve all fail-before and prior after evidence. A focused production-path test must show the new RHS equals `-S*i*omega*n_j`, the old sign fails, T6 body Neumann value remains green, and T6 Green-RHS orientation becomes green. T1–T5 stay green; T10 pressure traction and T7 coefficient extraction must remain red.
4. Do **not** change `compute_hydrodynamic_coeffs`, pressure/traction signs, A/B real-imag extraction, excitation force, diffraction RHS, outer/FS/sponge BC, mesh, RAO, damping closures, projection or clipping. The deliberately remaining traction defect means intermediate A/B may change sign and must not be labelled physically validated.
5. Retain the current T5 clean-after fingerprint/SHA as before baseline, then run the same five isolated frequencies with actual forced cache MISS and identical configuration/geometry/effective-BC contract. In addition to raw matrices/complex outputs/residuals, require `phi_rad_after ≈ -phi_rad_before` to floating-point tolerance and `phi_diff_after ≈ phi_diff_before`. With the unchanged linear pressure extractor, raw A/B should negate; excitation should remain unchanged. Any deviation requires explanation rather than relaxed tolerances.
6. Report RAO only as intermediate numerical impact. Do not interpret movement toward WAMIT, positive/negative damping, or RAO appearance as validation while T10/T7 remain failed. Convergence remains deferred.

#### CP4 post-change review 4 — T6 radiation Green-RHS sign

**Main/compatibility production-path decision:** `PASS`.

- The retained construction patch has SHA-256 `94002294df8085c534a9923e978de22b31b7e31b4d884b66f1468e1ba3ff4635` and changes exactly five files: a new shared RHS helper plus main-run, compatibility-run, cache-manifest and code-version integration. The only physical algebra change is `+S*(i*omega*n_j)` to `-S*(i*omega*n_j)`.
- `build_rankine_radiation_rhs` explicitly and consistently binds `exp(+i*omega*t)`, unit generalized displacement, `n_s=n_B=-n_Omega`, `q_s=i*omega*n_j` and `rhs=-S*q_s`. It does not alter normals, S, diffraction, post-solve potential signs, pressure/traction, A/B extraction or RAO.
- The actual `run_frequency_domain_case` radiation solve and the full-domain `solve_radiation_freq` compatibility entry call the same helper. The helper is in `get_rankine_code_version`; both base and per-frequency cache manifests bind the policy and hashes. The compatibility wrapper is not itself version-listed, but it has no independent potential-cache path, so helper fingerprinting is sufficient for this atomic physical change. The standalone key-builder file is not in the numerical-file list, but its serialized policy changes the key; no stale-key evidence appears in the clean runs.
- Focused evidence gives zero error for physical `q_s` and the new Green RHS, while the legacy sign retains relative error 2. T1–T5 remain green and T10/T7 retain the same explicit failures. `checkcode` is reported clean.
- All five T5-before/T6-after pairs at omega `{0.5,1.1,1.5,1.7,2.0}` are isolated forced cache misses. The before fingerprint is `25c49899...`, after is `d85ade404...`; configuration, Fine/body/component counts `588/560/1148/224`, matrix size 2520, seven geometry/source/collocation hashes and all effective BC values match.
- The exact zero identities are stronger than a trend comparison: radiation potentials, heave columns and unchanged-extractor raw A/B negate; diffraction potentials, incident potentials, complex excitation/F3 and both linear residuals remain unchanged. This is the required linear sign response. RAO movement is retained only as intermediate impact.

**Supported-entry/test-validity decision:** `PASS after focused blocker repair`.

- Initial review found that `Case_Wave/Benchmark_Symmetry.m` is an active exact full/half/quarter-grid validation command, not an inert archive, and still directly used the old positive RHS. This was a valid blocker because it could regenerate a physically opposite radiation solution while the first after suite reported T6 green.
- The accepted remedy was to route this active entry through `build_rankine_radiation_rhs` and execute that actual benchmark-local path in the focused test. Old `Case_Wave(Old）` and `Old Case Results` copies remain excluded archives; the final executable-path inventory must preserve that classification.
- `CRESTU_Theory_and_Technical_Manual.md` also shows the old positive-RHS code snippet twice. This is a documentation `WARNING`, not a second numerical blocker for this atomic patch, but it must be corrected before the final theory-to-code mapping is accepted.

Blocker-resolution amendment:

- The focused benchmark construction patch has SHA-256 `242206153fde1706078813c9fa204293c3a702f1ba27eb532a672ecec8eeb492` and changes only `Case_Wave/Benchmark_Symmetry.m`. Its normal solve now calls local `benchmark_radiation_rhs`, which delegates directly to production `build_rankine_radiation_rhs`; per-group helper metadata are retained in the benchmark result.
- The test executes the public benchmark entry with its audit dispatch, therefore entering the exact same local helper used by the normal solve. It records new-sign relative error 0 and old-sign error 2, and the local path itself hard-asserts that combination. This is sensitive to a regression and is not a source-text-only assertion. Both benchmark and after-test `checkcode` are reported clean.
- The helper, main solver, compatibility solver, cache policy and code fingerprint did not change. Consequently the existing five-frequency T6 clean comparison remains the valid physical before/after evidence; rerunning those ten expensive cases for this benchmark-only routing repair would add no main-solver information.
- The old directories are explicitly archival and excluded. The stale technical-manual snippets remain a documentation warning for final formula mapping, not a numerical or CP4 blocker.

No forbidden fitting, scaling, smoothing, WAMIT tuning, projection, clipping or plotting substitution is present. The clean evidence does **not** validate pressure damping or convergence.

#### Next change gate — T10/T7 physical body-traction sign

**Production authorization:** `PASS — limited to the single physical body-traction orientation issue and the five conditions below`.

With T6 coverage cleared, the admissible next atomic issue is narrowly defined as the physical generalized traction orientation, not a change to panel pressure quadrature or coefficient normalization:

1. Preserve `p=-i*rho*omega*Phi`, body outward normal/generalized normal `n_B=n_j`, panel areas, quadrature, symmetry weights and potential normalization. Implement body load as `Q=-integral(p*n_B dS)` in both radiation and wave-excitation load paths. Equivalently, radiation load becomes `+i*rho*omega*integral(Phi_R*n_j dS)` and excitation becomes `-integral(p_total*n_j dS)`.
2. Leave the coefficient definitions `A=Re(Q_R)/omega^2` and `B=-Im(Q_R)/omega`, unit-displacement radiation RHS, diffraction solve/RHS, FS/outer/sponge BCs, RAO equation, damping closures and raw/projected separation unchanged. Existing evidence isolates the sign before these divisions; do not bundle an unproven “extractor” rescaling.
3. A production-path after suite must turn T10 traction and T7 translating-sphere added mass green, keep the T10 sphere-quadrature and T7 zero-damping controls green, and keep T1–T6 unchanged. It must test both actual force routines; a duplicated local sign oracle is insufficient. Preserve the analytic tolerance rather than selecting it from WAMIT.
4. Reuse the current T6 clean state as before only with exact dependency SHA/fingerprint closure. Five isolated after runs must show potentials and linear residuals unchanged, radiation force/raw A/raw B exactly negated, pressure arrays unchanged, and complex excitation exactly negated (same magnitude, 180-degree phase change). Record RAO only as impact. Any failure of those hard identities is a blocker.
5. The user protected body-pressure integration from arbitrary changes. Authorization therefore extends only to the demonstrated final body-versus-fluid traction sign; it does not permit changes to pressure samples, areas, normals, quadrature, normalization, empirical damping, projection or plotting.

#### CP4 post-change review 5 — T10/T7 physical body-traction sign

**Decision:** `PASS`.

- The exact construction patch has SHA-256 `bcaf738ea20570f14e04812a19c4c6480a0d3d9c8a1e1cded0172863f16ea87e` and changes only `compute_hydrodynamic_coeffs.m` and `compute_wave_excitation.m`. The two physical lines implement the single traction-orientation issue: radiation `Q` changes from `-i*rho*omega*integral(Phi_R n_B)` to `+i*rho*omega*integral(Phi_R n_B)`, and excitation from `+integral(p n_B)` to `-integral(p n_B)`.
- This is not an accidental double negation. Under `exp(+i*omega*t)`, `p=-i*rho*omega*Phi`; `n_B` points body to fluid, so pressure traction on the body is `-p n_B` and generalized force is `Q=-integral(p n_B)`. For the unit-displacement sphere `Phi_R=-i*omega*a*cos(theta)/2`, the corrected mapping gives positive added mass `rho*V/2`, while the old mapping gives its negative. The already-corrected Green RHS determines the potential; the present patch changes no potential or BC.
- The focused test calls both production force routines on the mirrored existing Fine sphere (1176 full panels). Independent `integral(n_z^2)dS` quadrature is within 0.27525% of `4*pi*a^2/3`; returned pressure is exact; pressure traction and added mass have the same quadrature-limited 0.27525% error; radiation-force and unchanged A/B algebraic identities have zero error; pure-reactive sphere damping is zero. Both legacy force signs are rejected with relative error 2. T1–T6 remain green.
- The A/B extraction equations are unchanged and independently checked against the returned physical force. Calling this an “extractor rescale” would be wrong; evidence identifies the traction sign before `Re/omega^2` and `-Im/omega`.
- Five reused T6-before and five isolated after runs at omega `{0.5,1.1,1.5,1.7,2.0}` pass every clean contract: both sides are actual cache misses; Fine/component counts `588/560/1148/224`, N/matrix 2520, configuration, geometry hashes, effective BC and radiation-RHS metadata are identical; code fingerprints/cache keys differ. The dependency table changes only the two force files, both already in the code fingerprint.
- Every required identity closes exactly at all five frequencies: radiation, diffraction and incident potentials, pressure arrays, and radiation/diffraction residuals are unchanged; physical radiation force, raw A, raw B and complex excitation/F3 negate. F3 magnitude is unchanged and its phase moves by +/-180 degrees with maximum deviation `2.84e-14` degrees. RAO is retained only as numerical impact, not a sign oracle, validation or convergence result.
- Full-boundary potential diagnostics remain unavailable in schema 5 and RD-02 stays pending. This limitation is disclosed and no replacement diagnostic is fabricated.
- `CRESTU_Theory_and_Technical_Manual.md` still contains the old positive radiation RHS and old body-force signs in several code/formula snippets. This is a final theory-to-code documentation `WARNING`, not evidence against the inspected runtime patch, but all such mappings must be corrected before final acceptance.

No forbidden WAMIT scaling/tuning, empirical factor, smoothing, projection, clipping, curve fitting or plotting substitution appears in this patch or evidence.

**Next non-production gate (subsequently satisfied):** the manufactured T8 diffraction/incident cancellation and excitation-phase test passed through the production assembler/solve, force routine and profiler-observed active diffraction runner without a code change. Active watertight-frustum validation remains mandatory before OFAT factors 2/4. Flux and Haskind proceed under their independent DP gates and may not reuse one another.

#### CP4 post-change review 6 — active frustum instrumentation

**Decision:** `BLOCKER — do not start OFAT factors 2/4`.

Evidence that is accepted but insufficient to clear the gate:

- Optional `PARA12` input, active component rewriting, source-normal/ABC manifest, component/merged/cache hashes and code fingerprinting reach the real solver. The clean qtop=2 run is an actual forced MISS; N=2135 assembly and diffraction solve execute; `D+gamma_j*S` reconstruction is zero and the diffraction linear residual is `1.32e-14`.
- Absent PARA12 and explicit qtop=qbottom=1.5 return bitwise-identical component/merged arrays on the submitted configuration. Top/bottom/both active meshes have matching rings, edge multiplicity two, Euler characteristic two, one connected component, positive areas and consistent local winding. Physics formulas outside geometry/instrumentation remain frozen.

Blocking findings:

1. **Wrong Phase baseline.** The gate uses the body-study Fine configuration `PARA9 3/2/.../2`, giving body/FS/bottom/outer counts `588/280/868/112`, N=1848. Checkpoint 1 and all accepted five-frequency post-fix baselines use the current Fine outer configuration `PARA9 6/4/.../4`, counts `588/560/1148/224`, N=2520. Bitwise identity on the coarser object is useful backward-compatibility evidence, but it is not proof for the geometry to be swept. The baseline must be regenerated from the approved Fine outer config, then absent-versus-explicit-q1.5 identity and top/bottom/both active cases repeated.
2. **The accepted CP2 spacing rule is not implemented.** `commonThetaCount=ceil(...)` produces 69 nodes, not the required nearest multiple of four with a <=5% coarsening check. `verticalLayerCount=ceil(L_s/legacyWallTarget)` is also not the approved `round`-then-check, finer-ceil-on-exception rule. More importantly, `legacyWallTarget` comes from the coarser two-layer wall (25 m), not `h_s,target=50/4=12.5 m`.
3. **`resolutionPass` is one-sided and therefore false-green.** It only requires `actual <=1.05*target`; any amount of over-refinement passes silently. The submitted cases report top/bottom `h_rad/lambda=0.9747`, `h_wall/lambda=0.6310` and both `h_wall/lambda=0.9129` as pass. These values are not within the approved Fine target, and differences larger than 5% toward finer must be explicit integer-rounding exceptions, not silently called “resolution held.”
4. **Top and bottom radial resolution are not independently controlled.** Both surfaces use `outer_band_radial_target(freeSurface,...)`; the bottom's own baseline directional target is never measured. In top-only mode the fixed-radius bottom outer band is retessellated to satisfy common theta closure, which is an allowed derived consequence only if its physical radius/continuous area remain invariant and its actual top/bottom radial/tangential spacing remains on the respective fixed target or an explicit finer rounding exception. Those invariants are not present in the submitted table.
5. **Reported `h_rad` is not an independent active-mesh metric.** It is the maximum of `topInfo/bottomInfo.maximumRadialStep`, values derived from requested extension/layer counts or a removed legacy band. For top-only it is dominated by the retessellated fixed bottom band. It does not report measured top-extension and bottom-retessellation edges separately and can hide the changed annulus. Actual mesh vertices/edges must provide separate top/bottom new-band and retessellated-band P95/max radial values, top/bottom arc and chord values, wall slant/tangential values, `h/lambda`, `h/target`, aspect/skew and the exact rounding exception.
6. **The self-intersection oracle is not a self-intersection test.** `closed manifold + Euler 2 + connected + positive area + broad z/r bounds` does not exclude non-adjacent triangle/quad intersections or folded zipper annuli. O(N^2) brute force is not mandatory, but the replacement must be valid: for example AABB/spatial-hash broad phase plus robust non-adjacent triangle intersection, or encoded injectivity of the unwrapped ordered annulus/wall parameterization plus a cross-component intersection proof. Adjacent/shared-ring contacts must be excluded explicitly, and zero unintended intersections recorded for top/bottom/both.
7. **The zipper-triangle kernel check is only a finiteness check.** This is a `WARNING`, not an independent blocker here because the Rankine kernel was previously tested, but before relying on very skinny zipper elements the resubmission should report worst triangle quality and compare representative off-surface integrals with an independent quadrature/reference rather than equating finite positive output with accuracy.

Required resubmission contract:

- Use the approved `PARA9 6 4 20 40 1.12 4` Fine outer baseline at a representative high frequency and prove counts/hashes/arrays for absent versus explicit q=1.5. Preserve body hash and every frozen physics/BC input.
- Implement the exact CP2 rounding rule: fixed Fine top/bottom targets; `N_theta` nearest multiple of four, choose the finer multiple only if the nearest would be >5% coarse; `N_z=round(L_s/12.5 m)`, use ceil only when the rounded spacing is >5% coarse. Persist intended target, rounded candidate, chosen count, signed deviation and named exception per component/direction. No coarser-than-5% result is allowed.
- Recompute directional statistics from the final active vertices/edges, separately for top extension, fixed-side retessellation, bottom extension and wall. Retain derived construction values only as a cross-check. Prove the unchanged end radius and continuous surface area in top-only/bottom-only cases.
- Replace the self-intersection alias with a valid non-adjacent/cross-component intersection or injectivity oracle. Retain the existing watertight/Euler/connectivity/ring/winding/area/volume checks.
- Repeat top-only, bottom-only and both geometry gates, then one isolated approved-Fine active solve with actual MISS, complete hashes/manifests, production `D+gamma*S` column and residual evidence. All corrected state rows must be sensitive to deliberately bad rounding/intersection controls. Return the focused diff/evidence to Auditor; **no radius sweep before PASS**.

#### CP4 post-change review 6b — frustum blocker remediation

**Decision:** `PASS for instrumentation and BC execution; LIMITED PASS for diagnostic sweeps; not a pure radius OFAT`.

The six blocking defects in review 6 are cleared:

- The official Fine target is now the real `PARA9 6/4/.../4` object. Absent versus explicit q=1.5 is bitwise identical at body/FS/bottom/outer/total counts `588/560/1148/224/2520`; the earlier N=1848 case is retained only as an explicitly inactive backward-compatibility check.
- The implemented count rule uses the Fine wall target 12.5 m and separate top/bottom targets. The controlling theta count is the nearest multiple of four (68, signed deviation +0.832%); top/bottom wall `N_z=4` gives +3.682% slant deviation and the both-extended wall is exact. A deliberately coarser count is rejected by the production classifier.
- Resolution semantics are no longer one-sided. `resolutionHeld` is true only within symmetric +/-5%; all >5% refinement is named `roundingFinerException`, leaves `held=false`, and is retained in the evidence rather than silently promoted to held. Final active vertices/edges independently report nine component/band rows with P95/max radial, arc, chord, slant, tangential, `h/target`, `h/lambda`, aspect and skew metrics.
- Independent top/bottom physical radii and continuous opposite-surface area remain exact. Fixed-side retessellation is separately identified; component panel counts, hashes and matrix changes remain allowed derived closure outputs rather than hidden independent inputs.
- Full active meshes are checked with spatial-hash/AABB broad phase and non-adjacent triangle narrow phase: top/bottom/both have zero unintended intersections after about 1.09 million candidate pairs each. A deliberately folded wall produces 10 unintended intersections, demonstrating sensitivity beyond Euler/connectivity/area checks.
- The worst zipper triangle is compared against independent adaptive Duffy quadrature; potential and normal-derivative relative errors are `3.75e-16` and zero. One isolated qtop=2 approved-Fine production run is a genuine clean MISS at N=2752, with complete component/merged/gamma/cache manifests, zero `D+gamma*S` reconstruction error and diffraction residual `1.00e-13`. All 29 declared state rows and static checks pass.

No outer-ABC, free-surface, sponge, RHS, force, damping, RAO or projection physics was changed in this remediation. No empirical fit, scale factor, multiplier, smoothing, clipping or WAMIT tuning is present.

**Identifiability restriction:** `accepted=1` proves only “not coarser than the target”; it does not prove resolution is held. For top-only/bottom-only q=2 the fixed opposite ring is 17.633% finer tangentially than its baseline target; in both mode the new radial band is about 8.75% finer than target. These are valid and disclosed conforming-closure meshes, but their hydrodynamic deltas combine truncation shape with induced retessellation. Therefore:

1. The q={1.5,2,2.5} top-only and bottom-only runs may proceed at all five representative frequencies as **diagnostic top/bottom truncation-shape sensitivity**, with clean MISS, actual count/hash/cache data and every per-component resolution classification retained per row.
2. They may not be labelled pure `R_FS`/`R_bottom` OFAT, radius convergence, or evidence that radius alone is causal. A monotone q trend is not an asymptotic-convergence proof.
3. Any row whose exception pattern/count changes across q or frequency must be interpreted as mixed truncation/closure-resolution evidence. It must not be removed, smoothed or silently pooled with held rows.
4. To clear OD-01/OD-04 or make radius-only claims, Executor must add a matched-resolution/closure control or an explicit orthogonal/factorial comparison that quantifies the fixed-side retessellation effect. The required Radius x Resolution A/B/C study remains independent and cannot be replaced by these q sweeps.

Thus Checkpoint 3's active-frustum gate and Checkpoint 4's instrumentation patch are released. The remaining restriction is experimental identifiability, not solver executability.

#### CP4 post-change review 7 — PARA13 controls and raw diagnostics

**Decision:** `BLOCKER — do not freeze fingerprint 8cc68a... or start production cylinder sweeps yet`.

Accepted evidence:

- Legacy-absent control remains value-neutral. The official Fine explicit geometry is bitwise identical at `588/560/1148/224/2520`. Five fresh diagnostics-disabled runs at omega `{0.5,1.1,1.5,1.7,2.0}` are actual forced misses and are bitwise identical to the approved post-traction baseline in component/merged hashes, raw/projected A/B, complex excitation, pressure, RAO, body radiation/diffraction/incident component potentials and stored global residual scalars.
- The production mesh routing is real. `FS_RADIAL_COUNTS 6/4 -> 8/6` changes only FS (`560 -> 784`); the independent bottom no-write reference prevents FS output overwrite and leaves bottom fixed. Bottom counts change only bottom (`1148 -> 1372`); `Nz 4 -> 6` changes only outer (`224 -> 336`); transition radius changes FS+bottom. The deliberate FS+bottom two-field negative is rejected.
- The no-write bottom call has no file or active-FS side effect: `generate_free_surface_bmf(...,false)` constructs a separate returned mesh and gates its sole `write_bmf` call on `writeOutput`; component-hash isolation confirms the runtime result.
- The resolved/final manifest includes frequency, k/lambda, start/width/end/plateau, configured and actual mu, top/bottom radii, counts and component/merged hashes; it is included in cache/assembly metadata. Every changed configured control changes the cache key.
- Diagnostics are solver-value neutral. One clean w=1.5 N=2520 on/off pair has bitwise-identical physical arrays, primary body-potential arrays and geometry hashes. Eight nonempty radiation/diffraction x body/FS/bottom/outer algebraic partitions, solution norms and boundary potential maxima are finite. The labels correctly state `partitioned-algebraic-residual-Kphi-minus-rhs` and `physicalBCResidualStatus=NOT_COMPUTED_OFF_SURFACE`; no solved-row residual is presented as an independent BC residual.
- Raw and row/column-equilibrated MATLAB dense `rcond` estimates are computed without changing the production solve (`2.7596e-5`, `7.0168e-5` in the gate). Raw A/B diagonal, `sym(B_raw)`, minimum eigenvalue and reciprocity quantities remain independent of projected outputs. Checkcode is clean and the production dependency fingerprint is internally closed.

Blocking findings:

1. **The BC one-factor gate accepts a no-op.** `audit_phase2_2_one_factor` checks one configured field and geometry hashes, but its generic BC/diagnostic branch does not require the active numerical operator to change. In the submitted `ExplicitMuPolicy` pair both auto-tune flags are off, both `mu0` values are 0.35, and start/width/profile are identical. Changing the policy string from `LEGACY_FLOOR` to `EXPLICIT` therefore leaves `actualMu0`, the FS Robin vector, complex kappa/gamma and assembled matrix unchanged; only manifest text/cache key changes. This is exactly the prohibited “configuration changed but solved object did not” pattern. Policy-only must be classified configuration provenance/no-op, not a scientific OFAT pass. For start, width, mu0 and auto-tune, each run must persist and compare an actual FS-Robin coefficient hash and outer-gamma/operator hash. Diagnostics-only toggles must prove both hashes are identical.
2. **The separate full-Phi MAT has an unsafe cache-hit lifecycle.** Full Phi/RHS arrays are intentionally absent from the primary potential cache. With `PERSIST_FULL_PHI=1`, an `IPOTEN=0` hit does not reconstruct them, yet the unconditional final save truncates/overwrites the same-case raw-diagnostic MAT with `recordCount=0`. This can destroy valid clean evidence. Each record currently contains omega/group/problem/geometry hash but not its per-frequency cache key, configured/effective SHA or code fingerprint, so a stale file is not independently attributable to the active entry.

Required focused remediation:

- Add an active-operator contract. Persist an exact FS Robin/damping coefficient hash alongside the existing outer gamma hash. For sponge start/width/mu/auto candidates, the one-factor audit must require unchanged geometry and at least the intended operator hash delta; for diagnostics toggles it must require operator hashes unchanged. Mark policy-only same-operator cases `NO_EFFECT/NOT_A_SCIENTIFIC_LEVEL`, not PASS. Keep the existing two-input negative control.
- Make full-solution persistence clean-only and provenance-complete. The simplest acceptable policy is a hard stop when `PERSIST_FULL_PHI=1` with `IPOTEN=0`; alternatively, validate and preserve an existing file without rewriting it. Never save a zero-record file as successful diagnostics. Store per-record entry cache key, configured/effective manifest SHA, code fingerprint, matrix/component hashes and expected record count. Add a cache-hit negative/reload test proving the prior MAT SHA/record count is preserved or the requested operation is rejected before writing.
- Add a test-only conditioning calibration against at least one known-condition matrix plus singular/zero-row negative cases. This is a `WARNING`, not a separate production-code blocker, provided final reporting calls the outputs estimates and does not infer physical convergence from scaled-rcond improvement alone.
- The current controls expose radial FS/bottom counts and outer vertical resolution only. Cylinder sweeps may eventually use these named directional factors, but they may not be described as the complete FS/bottom panel-spacing or Radius x Resolution vector until azimuthal/tangential controls and actual `h/lambda` evidence required by Checkpoint 2 are present.

No production sweep is authorized on fingerprint `8cc68a...`; otherwise the required remediation would immediately invalidate its cache/code lineage. Test design and case-generation dry runs may continue. Return a focused diff plus no-op/operator-hash and cache-hit persistence negatives for re-review.

#### CP4 post-change review 7b — instrumentation remediation

**Decision:** `PASS — fingerprint freeze and limited cylinder sweeps authorized`.

The two blocking findings from review 7 are cleared:

- Production now shares the exact FS coefficient helper between cache preview and assembly: `nu_j=(omega^2/g)*(1-i*mu_j)`. Exact FS radius/damping/Robin hashes, outer gamma hash, geometry hash and source-orientation hash form a boundary-operator identity that is bound into the finalized effective manifest and per-frequency cache key. Assembly independently rebuilds it and hard-fails on preview mismatch.
- Start, width, mu0 and auto-tune controls have identical geometry but actual changed FS/operator hashes. Diagnostics toggles have identical geometry and operator hashes. The former policy-only pair has identical actual operator, returns `NO_EFFECT/NOT_A_SCIENTIFIC_LEVEL`, `operatorPass=0`, `reportPass=0`, and is counted as an expected rejected level rather than a positive scientific run. The two-independent-field negative remains rejected.
- `PERSIST_FULL_PHI=1` with `IPOTEN=0` now raises `CRESTU:Phase22DiagnosticCleanRequired` during configuration parsing, before domain generation or solver/result writes. The real compatible-cache/sentinel negative changes only IPOTEN and proves identical RawDiagnostics bytes/SHA, potential-cache SHA and results SHA. Full records now bind cache key, configured/effective SHA, code fingerprint, matrix size, all component/merged geometry hashes and source/collocation hashes.
- Conditioning terminology is corrected to `ESTIMATE_AVAILABLE/UNAVAILABLE`. Exact 0.125 and `1e-10` diagonal reciprocal conditions are reproduced; a singular zero-row case returns zero and unavailable. Metadata explicitly forbids a physical-accuracy or convergence inference from raw/scaled changes.
- Two new isolated Fine w=1.5 off/on runs are actual N=2520 misses with identical component/merged and boundary-operator hash `9edec608...`; raw/projected hydrodynamics, complex excitation/pressure/RAO and primary potentials are bitwise identical. The enabled run retains two provenance-complete full records. Fingerprint `9470060d74aba0e42d12ac6e40faa43421494586a0e3d6b97e9576cd8fbc321a` is supported by the submitted 34-file dependency table and focused static checks.

The missing recovered end-to-end enabled timer is a transparent `WARNING`, not a physics/cache blocker: the clean solver log, assembly runtime and outputs predate only a corrected post-analysis assertion. Do not use this pair for performance claims. Historical expected-fail aggregate scripts that are no longer replayable against the final code remain archive evidence only; current production gates and negative controls are the acceptance basis.

**Authorized scientific factor names on the frozen cylinder path:**

1. `FS_RADIAL_PANEL_COUNT` — vary only `FS_RADIAL_COUNTS`; this is radial resolution, not complete FS panel spacing.
2. `BOTTOM_RADIAL_PANEL_COUNT` — vary only `BOTTOM_RADIAL_COUNTS`; this is radial resolution, not complete bottom panel spacing.
3. `OUTER_VERTICAL_PANEL_COUNT` — vary only outer `Nz`; this is vertical wall resolution, not tangential wall resolution.
4. `SPONGE_START_RADIUS` — fixed geometry, width, mu0 and auto policy; require actual FS/operator hash change.
5. `SPONGE_WIDTH` — fixed geometry, start, mu0 and auto policy; require actual FS/operator hash change.
6. `SPONGE_MU0` — damping-strength factor with auto-tune disabled and all geometry/start/width fixed.
7. `SPONGE_AUTO_TUNE_POLICY` — optional policy-sensitivity diagnostic only, with actual per-frequency mu and operator hashes reported; do not pool it with the fixed-mu0 sweep.
8. `MESH_TRANSITION_RADIUS` — supplemental shared FS+bottom grading-transition diagnostic, explicitly not FS/bottom physical radial extent and not one of the nine required pure factors.

`RAW_DIAGNOSTICS`/`PERSIST_FULL_PHI` are instrumentation settings and must be fixed across compared levels, not treated as physics factors. `SPONGE_MU_POLICY` label-only changes are prohibited as a sweep. This authorization excludes pure FS/bottom radial extent, common outer radius, top/bottom frustum radius-only convergence, tangential/azimuthal resolution, full Radius x Resolution A/B/C, flux/Haskind and any WAMIT-selected level. The earlier frustum limited diagnostic authorization is unchanged and remains non-radius-identifiable.

**Fingerprint discipline:**

- Every official sweep run must use and record exact fingerprint `9470060d...` plus the 34 dependency SHAs. Use isolated case/output directories, assert all generated result/cache/raw-diagnostic paths absent, set `IPOTEN=1`, and require observed cache `MISS`.
- Freeze Fine body count/hash, all non-factor independent inputs, diagnostics flags, physical formulas and operator conventions. Every candidate must pass the one-independent-input report, component-hash whitelist and expected operator-effect contract at every frequency.
- Persist actual panel counts, matrix size, component/merged/source/collocation hashes, configured/effective SHA, FS/damping/gamma/operator hashes, k/lambda, directional `h/lambda`, cache key/status and the required raw diagnostics.
- Do not place data from fingerprints `87d68e...`, `8cc68a...` or any other code state into a `9470060d...` convergence sequence. They may appear only as clearly labelled historical before/after evidence.
- Any production dependency change stops the sweep. Recompute the fingerprint/dependency table, return the diff to Auditor, and cleanly rerun every affected baseline and level; do not splice pre/post rows. Test-only presentation changes do not alter the solver fingerprint but still require evidence integrity.
- A monotone result, a smaller scaled-rcond estimate or movement toward WAMIT is not convergence. Current authorization is execution permission, not acceptance of any conclusion.

#### CP4 post-change review 8 — physical BC / flux / Haskind separation

**Decision:** `BLOCKER — diagnostic mathematics accepted; final sweep fingerprint not frozen`.

The new diagnostic algorithms and their negative validation results are technically credible:

- The off-surface representation is consistent with `G=1/r`, `Delta G=-4*pi*delta` and canonical source normal `n_s=-n_Omega`: `Phi=int[(dG/dn_s)Phi-G*q_s]dS/(4*pi)`. Radiation body `q_s=i*omega*n_j`, FS `q_s=-nu*Phi`, bottom zero and outer `q_s=gamma*Phi` match the accepted production operator. Constant/harmonic closed-surface manufactured fields, refinement and wrong-normal negatives exercise the shared production panel integrals and orientation helper rather than a copied kernel.
- Under `exp(+i*omega*t)` and peak complex amplitudes, the diagonal pressure-work identity is correct: `p=-i*rho*omega*Phi`, `P_out=0.5*Re int(p*q_out*)dS`, and unit generalized displacement gives `B=2*P_out/omega^2`. The finite-depth plane-wave/group-velocity oracle converges to relative error `4.21e-15`; the reversed normal produces negative outward power. B33 has the translational damping dimension kg/s. For cross modes the raw matrix index convention is the transpose of the conventional `xi^H C xi` ordering, but `real(0.5*(C+C^H))` is invariant under that transpose, so the returned real symmetric damping and the Hermitian-residual norm are unaffected. The final report should state this indexing explicitly.
- The control cylinders lie between the body and sponge start, use outward radial derivatives, integrate the complete finite-depth side surface and preserve the raw work/unsymmetrized matrices before the real-Hermitian presentation. The returned w=1.5 number is properly rejected: radius variation is 23.6% and Hermitian residual 70.8%, despite quadrature and radial-derivative deltas passing. Thus `B_flux=62240.4` is a stored diagnostic candidate, not a validated closure.
- The selected-epsilon BC partitions are useful and non-circular with respect to `K*Phi-rhs`. Their status is accepted with a `WARNING`, not as continuum verification: the 25% P95 threshold is permissive and uses a per-column global normalization, while actual P95 values are much smaller (<=2.90%) and epsilon sensitivity is <=0.869%. Reports must say “selected-epsilon off-surface diagnostic passed” and must not say the continuous BC is converged or fully validated.
- Clean Fine w=1.5 OFF/ON runs are actual misses, retain N=2520 and identical geometry/operator hashes, and leave the physical solver arrays and primary potentials bitwise unchanged. Three provenance-complete diagnostic records are persisted. The Analyzer's `pass=1` is an integrity-gate pass only; the explicit flux/Haskind statuses remain `NOT_VALIDATED`.

One semantic blocker remains. Production still stores the unvalidated Haskind outer-product candidate through `B_energy_all` and exposes it as `results.damping_energy`, `diagnostics.energy_damping`, `pressure_energy_relative_residual`, and `B33_pressure_energy_relative_residual`. Those unqualified scientific field names permit exactly the energy/Haskind conflation prohibited by the requirement, even though newer metadata and `B_haskind_candidate` are correct. The compatibility function's corrected header is not sufficient because the top-level results alias has no adjacent mandatory status.

**Required remediation before sweep freeze:**

1. Make the active scientific path and internal variable names Haskind-candidate-specific. The runner must not treat or compare this candidate as energy flux.
2. Remove the ambiguous top-level/diagnostic “energy” fields from the current schema, or isolate them under an explicitly deprecated compatibility namespace whose status states `HASKIND_CANDIDATE_NOT_VALIDATED_NOT_ENERGY_FLUX`. A compatibility wrapper may remain, but the production runner must call the correctly named path and no sweep/report may consume the legacy alias.
3. Add a focused semantic negative proving no active output/analysis path classifies the candidate as energy flux, while `B_pressure_raw`, `B_flux`, `B_haskind_candidate` and all validity statuses/ratios remain distinct. Rename the Analyzer's generic pass to an integrity-specific field or document it equivalently in the machine-readable summary.
4. Because the runner/code dependency changes, issue a new code fingerprint/dependency table. One isolated Fine w=1.5 clean MISS is sufficient for this semantic-only patch if it proves all solver potentials, pressure/raw/projected matrices, excitation/RAO, the three stored numerical damping values and physical diagnostic records are bitwise identical to fingerprint `600e6d02...`; the only allowed changes are names/schema/status metadata. Any numerical change reopens the full physics gate.

Until this is cleared, fingerprint `600e6d02...` is not authorized for final cylinder sweeps. The previous limited-factor authorization remains the intended scope after a replacement fingerprint passes: FS radial count, bottom radial count, outer vertical count, sponge start/width/mu0, optional actual-operator auto-policy diagnostic and supplemental transition diagnostic. Pure radius attribution, tangential resolution and full Radius x Resolution A/B/C remain separately gated.

#### CP4 post-change review 8b — semantic-isolation resubmission

**Decision:** `BLOCKER — core schema/runner remediation passes; one active convergence consumer was omitted`.

The originally identified aliases are cleared in the core path:

- Results schema 7 has no `damping_energy`; frequency diagnostics have no `energy_damping`, `energy_diagnostics`, `pressure_energy_relative_residual` or `B33_pressure_energy_relative_residual`. The primary fields are `haskind_damping_candidate`, `B_haskind_candidate` and the explicit closure statuses.
- `run_frequency_domain_case.m` and `Benchmark_Symmetry.m` call `compute_haskind_damping_candidate` directly. Its metadata is `NOT_VALIDATED`, `isHaskindCandidate=true`, `isEnergyFlux=false`. The old `compute_radiation_damping_energy.m` is a wrapper whose return metadata is nested under `deprecatedCompatibility`; no active core solver or benchmark calls it.
- The machine source gate proves the independent B-flux source has no Haskind call and that the old function name occurs only in the wrapper and fingerprint list within the directories it scans. The evidence summary now says `evidenceIntegrityPass` and explicitly `does-not-validate-B-flux-or-Haskind`.
- The replacement Fine w=1.5 run is an isolated clean MISS on fingerprint `78aff7cdcce4b77f1045cd4be27913e505ee33f39fda9336063346f3759ee911`, N=2520. Against the accepted `600e6d02...` numerical baseline it reports bitwise identity for physical arrays, primary potentials, geometry/operator state, Haskind candidate, closure, physical-BC records and B-flux records. B-flux and Haskind remain `NOT_VALIDATED`. This satisfies the requested semantic-only value-neutral comparison.

However, repository-wide inspection found an active unscanned consumer:

- `Case_Wave/Case1_SingleSphere_Convergence/Run_SingleSphere_Convergence.m` is not an archive; the latest audit generator identifies it as the production convergence entry. Its clean/reload comparison still requests `damping_energy`, and its diagnostics table still reads `pressure_energy_relative_residual` and `B33_pressure_energy_relative_residual`. Schema 7 removed all three fields, so this entry will error if run and continues the prohibited energy/Haskind terminology.
- The semantic source gate scans all `Source Code/**/*.m`, plus the main runner and benchmark, but it does not scan this convergence entry. Consequently `sourceCallIsolation=1` is true only for an incomplete target set and cannot close the repository-level active-consumer requirement.
- `Write_Numerical_Audit_Report.m` also still generates the sentence that an independent `damping_energy` is saved. It must either be explicitly marked/guarded as an immutable schema-6 Phase-2.1 historical generator or updated so it cannot regenerate a current report with obsolete semantics.

**Required focused remediation:** update `Run_SingleSphere_Convergence.m` to compare `B_haskind_candidate` under candidate-specific names and read the new pressure-versus-Haskind-candidate diagnostic fields, without promoting them to validated energy or flux. Extend the semantic gate to scan active `Case_Wave` consumers for both the legacy function name and the removed field tokens; archived Phase-2.1 evidence/tests may be whitelisted explicitly. Update or explicitly historical-guard the old audit-report generator. A schema-7 fixture/analyzer test plus checkcode is sufficient; the 319.6 s solver run need not be repeated because these files are outside the solver/cache fingerprint and no numerical production dependency should change.

Fingerprint `78aff7...` is technically supported as the current solver fingerprint, but it is **not yet frozen/authorized for final sweeps** while its named convergence consumer is broken and the isolation gate omits it. Once the focused consumer/gate remediation passes without changing a fingerprinted dependency, `78aff7...` may be frozen under the same limited-factor scope already stated; otherwise any fingerprint change requires a replacement lineage.

#### CP4 post-change review 8c — active-consumer resubmission

**Decision:** `BLOCKER — semantic fields are corrected, but the actual convergence wrapper still fails and the injection negative is not effective`.

Accepted portions:

- `Run_SingleSphere_Convergence.m` now delegates clean/reload comparison and first-order diagnostics to schema-7-only helpers. The helpers require schema 7, compare `haskind_damping_candidate`, emit pressure-versus-Haskind-candidate columns and reject schema-6 active use. No prohibited energy-named field remains in the active Case_Wave scan.
- `normalize_audit_haskind_semantics` accepts an internally consistent schema-7 candidate table, rejects mixed schema/field semantics, and maps only an all-schema-6 Phase-2.1 fixture under `deprecatedCompatibility` with `PHASE2_1_HISTORICAL_HASKIND_CANDIDATE_NOT_VALIDATED` and “not B_flux” metadata.
- The seven-row fixture covers helper clean/reload, diagnostic-table columns, current report mapping, historical mapping, schema/field mismatch, numerical mismatch and schema-6 active-consumer rejection. Checkcode and dependency SHA evidence are present. None of the edited convergence/report/test files belongs to `get_rankine_code_version`'s solver dependency list, so not repeating the 319.6 s physical run is reasonable **after** the focused consumer blockers below are fixed.

Two concrete test/consumer defects remain:

1. In `Run_SingleSphere_Convergence.m`, `compare_clean_and_reload` assigns only `comparisonTable = compare_schema7_clean_reload_results(...)` but then executes `assert(all(withinMachinePrecision),...)`. `withinMachinePrecision` no longer exists in this subfunction. The active convergence entry therefore still fails even when every schema-7 comparison passes. The assertion must use `comparisonTable.withinMachinePrecision` (or the helper must assert internally), and the fixture must exercise this actual wrapper contract rather than only the standalone helper.
2. `legacy_token_negative_fixture_is_detected` is not an injected scanner negative. The code constructs a string already containing a forbidden token and calls `contains` directly; it never places a fixture in the scan target nor invokes the same violation-collection logic. Refactor the scanner predicate into a shared helper or scan a temporary active-path fixture, prove a violation is returned, then remove/clean it. The current row proves only MATLAB string matching.

One documentation correction is also required before final reporting: the active `Write_Numerical_Audit_Report.m` still states radiation RHS as `S*(i*omega*n_j)`, while the accepted production convention is `-S*(i*omega*n_j)`. This stale formula was already identified in the T6 review and must not be regenerated into the current audit report. Its correction is report-only and does not require a physical rerun.

After these focused fixes, rerun the schema-7 consumer/source-isolation gates and checkcode/SHA only. If fingerprinted production files and numerical evidence remain unchanged, fingerprint `78aff7...` can then be frozen for the previously authorized limited outer-domain factors. It is not authorized yet.

#### CP4 post-change review 8d — focused consumer/test remediation

**Decision:** `PASS — fingerprint 78aff7... may be frozen; CP4 no longer blocks sweep design`.

All three focused blockers are closed:

- The active convergence entry now delegates clean/reload enforcement to `write_schema7_clean_reload_verification`; the shared function asserts `all(comparisonTable.withinMachinePrecision)`. The executable fixture calls that same shared wrapper, writes the expected CSV, obtains 7/7 positive rows, perturbs the Haskind candidate, and observes `CRESTU:CacheReloadPrecision`. There is no undefined local variable or helper-only false validation.
- Active Case_Wave scanning and the negative fixture both call the same `collect_legacy_token_violations` predicate. The injected token produces a recorded violation; active non-archive consumers have none. `Phase2_2_Artifacts` and `Old` are explicit archive markers. The nine-row source-isolation CSV passes, and independent `rg` review finds no active legacy fields/functions outside the permitted deprecated wrapper/fingerprint list.
- The current report formula chain is consistent with the accepted convention: `exp(+i*omega*t)`, body `n_s=n_B=-n_Omega`, radiation `q_s=i*omega*n_j` and `rhs=-S*q_s`; diffraction `q_D=-q_I` and `rhs=+S*q_I`; `p=-i*rho*omega*Phi`, `Q_R=-int(p n_B)=+i*rho*omega*int(Phi n_B)`, `A=Re(Q_R)/omega^2`, `B=-Im(Q_R)/omega`, and excitation `F=-int(p_total n_B)`. The previous stale positive radiation-RHS statement is removed.

The 23-file checkcode table has zero issues and the 23-entry dependency SHA table covers the new consumer/report/test helpers. These focused files are outside `get_rankine_code_version`'s numerical/cache dependency list; production solver files, the accepted clean result and diagnostic payloads did not change. Therefore preserving the prior physical run without another 319.6 s solve is justified.

**Fingerprint authorization:** freeze `78aff7cdcce4b77f1045cd4be27913e505ee33f39fda9336063346f3759ee911` as the current Phase-2.2 solver fingerprint. Any later change to a fingerprinted numerical dependency invalidates this freeze and requires a replacement baseline/lineage.

**Sweep authorization boundary:** CP4 no longer prevents resuming the orthogonal-design work and preparing official cases. This PASS does not clear the independent experimental-design blockers OD-01/OD-04 or approve an unreviewed Radius x Resolution matrix. Formal sweep execution remains conditional on the exact run manifest/design being approved and must retain the previously stated clean-MISS, fixed-Fine-body, one-factor, actual-hash/operator, directional `h/lambda` and provenance contracts. Existing permitted factor names remain FS radial count, bottom radial count, outer vertical count, sponge start/width/mu0, actual-operator auto-policy diagnostic and supplemental transition diagnostic; frustum rows remain truncation-shape sensitivity unless their retessellation confound is controlled.

### Checkpoint 5 — Final acceptance

Reconcile every checklist item against actual artifacts/data, answer Q1–Q6, audit conclusions against raw data, enumerate unresolved blockers, and emit final Auditor state variables. Only `FINAL_AUDIT = PASS` permits Phase-complete language.

**Current verdict:** `PENDING`.

## 11. Auditor risk watchlist

1. Stale result/potential cache or a cache key that does not include the varied factor.
2. Config changed but generated active panels/BC coefficients did not; mesh hashes or matrix rows stay unexpectedly identical.
3. A “resolution” run also moves radii, sponge start, zone width, or other component spacings.
4. Nominal spacing reported instead of actual mesh statistics and `h/lambda` at each frequency.
5. Small linear algebra residual mistaken for accurate boundary physics; collocation residual circularly restates the solved rows.
6. Pressure/flux/Haskind paths share arrays/prefactors or one is overwritten, so they are not independent.
7. Reciprocity projection, PSD clipping, smoothing, or selected plots hide raw signs/eigenvalues/outliers.
8. Closeness to WAMIT is labelled convergence or used to choose numerical parameters.
9. Condition estimates are unreliable/unlabelled, or worsening refinement is rationalized without local residual/potential evidence.
10. Final causal language exceeds experiment identifiability; an unresolved mixed cause is presented as a unique root cause.

## 12. Decision log

| Time (Asia/Shanghai) | Checkpoint | Decision | Summary |
|---|---|---|---|
| 2026-08-30 | Checklist initialization | PASS | Requirements captured; latest numerical audit read; production tree intentionally untouched by Auditor. Awaiting Executor plan for Checkpoint 1. |
| 2026-08-30 | Checkpoint 1 — Executor plan | PASS | Plan covers fixed Fine-body clean baseline at omega {0.5, 1.1, 1.5, 1.7, 2.0}, all nine OFAT factors, effective-geometry/cache provenance, wavelength normalization, three radius×resolution groups, `exp(+iωt)` BC audit, three independent damping paths, raw/local diagnostics, conditioning, WAMIT-only validation, single-change evidence gates, and required deliverables. Six carry-forward warnings are explicit under Checkpoint 1; none currently blocks design work. |
| 2026-08-30 | Checkpoint 2 — Outer-domain design | BLOCKER | Clean/cache authenticity, directional `h/lambda`, sponge start/width independence, and radius×resolution groups pass design review. Replacing physical FS/bottom radial extents with fine-resolution-zone endpoints does not meet two explicit OFAT requirements. A valid independently controlled closed-domain parameterization plus derived connector BC, or an explicit unresolved requirement/scope decision, is required. |
| 2026-08-30 | Checkpoint 2 resubmission — Outer-domain design | PASS | The physical extent gap is resolved by independent top/bottom frustum radii with deterministic closure effects. Derived meridional/tangential counts preserve actual wall spacing and prevent a radius-resolution confound. Frustum topology/metric assertions are explicit. Its projected finite-depth outgoing BC remains a mandatory Checkpoint 3 gate before factors 2/4 may run. |
| 2026-08-30 | Checkpoint 3 — BC mathematics | BLOCKER | Independent Green derivation confirms `D_s=1/2-K_s` requires inward source normals and RHS `-S q_s`. Current FS orientation, radiation RHS, finite-depth FS coefficient, outer S sign/decay/sponge consistency, and body-force signs are inconsistent; radiation RHS and force errors can double-cancel. Diffraction body RHS, bottom zero flux, incident time convention and RAO matrix form are analytically consistent. Prioritized T1–T11 gates are recorded; no production change or outer experiment is yet accepted. |
| 2026-08-30 | CP4 pre-change authorization 1 | PASS | Fail-before suite truthfully reports 10/19 physical passes and isolates mixed FS double-layer orientation with actual Rankine kernel/closed-cube controls. Authorized only per-source-column FS `dGdn` canonicalization plus alignment assertions and cache/assembly metadata. Separate production-path after evidence must turn only T2 green; full CP4 remains pending. |
| 2026-08-30 | Checkpoint 4 — orientation patch post-change | BLOCKER | Commit `938fb91` and 13-row after suite pass focused code/unit review; component ordering, winding, image handling, self term and cache binding are correct. The mandatory five-frequency clean numerical before/after rerun is absent, so full CP4 cannot pass and no next production patch is authorized. |
| 2026-08-30 | Checkpoint 4 — orientation clean evidence resubmission | PASS | Ten isolated Fine-body runs close the prior evidence blocker: all are actual forced cache MISS, all five pre/post pairs have identical active geometry/configuration/effective values and distinct code/cache fingerprints, and raw matrices/complex force/RAO/residual/potential data are retained. Large changes are labelled impact only. Convergence is explicitly deferred while known analytic defects remain; no convergence claim is accepted. |
| 2026-08-30 | CP4 pre-change authorization 2 | PASS | T3 analytically isolates the finite-depth FS Robin base error (`k` versus `omega^2/g`). Authorized only the named base-scale replacement `nu0=omega^2/g` in `nu_FS=nu0(1-i mu)`, production-path metadata/test, and matching five-frequency clean before/after evidence. Sponge profile/mu, outer BC, radiation RHS and force signs are excluded. |
| 2026-08-30 | Checkpoint 4 — finite-depth FS Robin post-change | PASS | The three-file production patch exactly matches authorization. An actual assembler column passes `D-nu0*S`, 18/18 test-state rows match, complete clean/fingerprint/SHA baseline reuse is demonstrated, and five after runs are isolated MISS with unchanged geometry/outer controls. Large low-frequency impact is reported; negligible high-frequency impact rules this defect out as the principal high-frequency cause without claiming convergence. |
| 2026-08-30 | Next change gate — T5 outer outgoing BC | BLOCKER | Production patch not authorized. Complex-kappa branch/root validation, exact-versus-asymptotic cylinder evidence, actual assembled outer-column fail-before, and frustum projected-normal manufactured test are required first. Read-only derivation and test construction are authorized. |
| 2026-08-30 | T5 outer outgoing BC evidence resubmission | PASS | Twenty complex-root continuations, mode-dependent exact Hankel controls, monotonically improving local-asymptotic errors, five real production-column fail-before cases and forty actual-winding frustum projections satisfy the pre-change gate. Authorized only a named first-order local asymptotic absorbing closure with actual effective outer mu, panel-local radius/normal projection, canonical `D+gamma*S`, complete cache/code binding and clean after evidence. It must not be represented as exact/modal DtN. |
| 2026-08-30 | Checkpoint 4 — T5 local asymptotic outer ABC post-change | PASS | The five-file patch exactly implements the authorized non-exact local closure. Complex-root hard-stop behavior, actual panel-center mu, source-local normal/gamma signs, production-column assembly, preview equality, cache/version binding, unit-state preservation and five-frequency clean comparison pass. Its small high-frequency impact rules out this one legacy row formula as the 96% cause on the fixed grid, not outer resolution/truncation/sponge coupling or convergence. |
| 2026-08-30 | CP4 pre-change authorization 4 — T6 radiation RHS | PASS | Authorized only `rhs=-S*(i*omega*n_j)` for unit generalized displacement under the canonical stored body normal, plus explicit cache/normalization metadata and production-path/clean sign-flip evidence. Pressure traction/extraction, diffraction, BCs, RAO and damping closures are excluded and T10/T7 must remain red. |
| 2026-08-30 | Checkpoint 4 — T6 radiation RHS post-change | BLOCKER | The five-file main/compatibility patch, helper/cache binding, focused unit evidence and five-frequency clean sign identities pass. However `Benchmark_Symmetry.m` remains an advertised active validation entry and still solves with the obsolete positive RHS, so test/entry coverage is incomplete. Route it through the shared helper (preferred) or formally deprecate it before CP4 can pass. Stale positive-RHS snippets in the technical manual are a final-documentation warning. No T10/T7 production change is authorized until this blocker clears. |
| 2026-08-30 | Checkpoint 4 — T6 active-benchmark resubmission | PASS | The one-file focused benchmark patch routes both the normal solve and executed audit dispatch through the production RHS helper, records per-group policy, returns new-sign error 0 and rejects the old sign with error 2 under a hard assertion. Main/helper/cache/fingerprint code is unchanged, so the accepted five-frequency clean comparison remains applicable. T10/T7 body-traction orientation is now authorized under its narrow five-condition gate. |
| 2026-08-30 | Checkpoint 4 — T10/T7 body-traction post-change | PASS | The two-line/two-file patch implements `Q=-integral(p n_B)` without changing pressure or A/B mapping. Fine-sphere quadrature/traction/added-mass controls pass, old signs are rejected, T1–T6 remain green, and five clean pairs satisfy exact unchanged-potential/pressure/residual plus negated-Q/A/B/F identities. Checkpoint 3 remains blocked only by T8 end-to-end diffraction/excitation and the active watertight-frustum gate; flux/Haskind are separate final damping-closure blockers. |
| 2026-08-30 | Checkpoint 3 — T8 diffraction/excitation gate | PASS | No production change. Uniform-flow sphere assembly/solve, exact/off-surface Neumann controls, analytic excitation, finite-depth incident normalization and profiler-observed clean active diffraction path all pass; opposite RHS and old traction remain sensitive failures. The T8 blocker is cleared. Overall Checkpoint 3 remains BLOCKER only for the active watertight-frustum path needed by factors 2/4; cylinder OFAT/diagnostics may begin under the frozen-fingerprint contracts. |
| 2026-08-30 | Checkpoint 4/3 — active frustum instrumentation | BLOCKER | Real topology/cache/assembly execution is demonstrated, but the test uses the wrong `588/280/868/112`, N=1848 outer grid instead of the approved `588/560/1148/224`, N=2520 Fine baseline. The one-sided resolution predicate accepts arbitrary refinement, uses FS target for bottom, violates the multiple-of-four/round-then-check rule and reports derived aggregate h rather than actual per-band metrics. Its “self-intersection” row does not test intersection/injectivity. Corrected Fine-baseline and topology evidence is mandatory; factors 2/4 remain prohibited. |
| 2026-08-30 | Checkpoint 4/3 — frustum remediation | PASS / LIMITED SWEEP | The official Fine baseline, exact two-sided rounding semantics, independent top/bottom targets, final-vertex directional metrics, robust intersection negative control, independent zipper quadrature and isolated N=2752 production solve clear the geometry/BC/instrumentation blockers. Diagnostic q sweeps may run, but fixed-side tangential retessellation is 17.633% finer and some radial bands have >5% finer exceptions; results must be labelled truncation-shape sensitivity. OD-01/OD-04 remain blocked against pure radius attribution until a matched/orthogonal confound control is supplied. |
| 2026-08-30 | Checkpoint 4 — PARA13/raw diagnostics | BLOCKER | Mesh controls, no-write bottom isolation, manifests, five-frequency disabled value neutrality, honest algebraic partitions and one clean enabled diagnostic run pass. The generic BC gate nevertheless calls a policy-only same-operator change an OFAT pass, and an `IPOTEN=0` persistence request overwrites the separate full-Phi MAT with zero records lacking per-entry cache/effective provenance. Add FS-Robin/operator hashes and no-op classification; hard-stop or preserve cache-hit full diagnostics with provenance. Do not freeze fingerprint `8cc68a...` or start production sweeps before resubmission. |
| 2026-08-30 | Checkpoint 4 — instrumentation remediation | PASS / LIMITED SWEEPS | Actual FS/damping/gamma/operator hashes reject the policy-only no-op and validate intended BC changes; a real cache-hit sentinel negative proves early hard rejection and byte preservation; full records carry complete cache/config/code/mesh provenance; condition-estimate semantics and analytic calibrations pass; current-code N=2520 off/on clean misses are bitwise value-neutral. Freeze fingerprint `9470060d...`. Authorized only named radial FS/bottom, vertical outer, sponge start/width/mu0, optional auto-policy and supplemental transition diagnostics under strict clean/fingerprint contracts; no pure radius/tangential/full orthogonal claim is implied. |
| 2026-08-30 | Checkpoint 4 — physical BC / flux / Haskind separation | BLOCKER | Off-surface representation, canonical q signs, pressure-work sign/prefactor, finite-depth plane-wave oracle, independent control-cylinder storage, selected-epsilon partitions, clean value neutrality and provenance pass implementation review. B_flux and Haskind are correctly NOT_VALIDATED. Freeze is denied because the same Haskind candidate is still exposed through unqualified `damping_energy` / `energy_damping` / pressure-energy fields, allowing the prohibited energy/Haskind conflation. Semantic isolation/deprecation, a machine-readable negative and a replacement fingerprint with one clean value-neutral Fine run are required. |
| 2026-08-30 | Checkpoint 4 — semantic-isolation resubmission | BLOCKER | Core schema 7, main runner, benchmark, deprecated wrapper, machine statuses and clean w=1.5 bitwise comparison pass. Repository inspection finds the active `Run_SingleSphere_Convergence.m` still reading removed `damping_energy` and pressure-energy fields; it will fail on schema 7 and was omitted from the source-isolation gate. The old audit generator can also regenerate obsolete semantics. Fix/guard these active consumers and expand the gate; no 319.6 s rerun is required if fingerprinted dependencies remain unchanged. Do not yet authorize final sweeps on `78aff7...`. |
| 2026-08-30 | Checkpoint 4 — active-consumer resubmission | BLOCKER | Schema-7 helper semantics, historical guard, active token scan and seven fixture assertions largely pass, and no physical rerun is needed for non-fingerprinted consumer-only edits. The actual convergence wrapper nevertheless asserts undefined `withinMachinePrecision` after delegating to the helper, so the active entry still fails. The claimed injected-token negative only calls `contains` on a token-bearing string and does not exercise the scanner. The current report generator also retains the obsolete positive radiation RHS. Fix these focused issues and rerun only consumer/source/checkcode gates; do not freeze `78aff7...` yet. |
| 2026-08-30 | Checkpoint 4 — focused consumer/test remediation | PASS / DESIGN GATE REMAINS | The shared schema-7 wrapper now asserts its returned table and its executable positive/negative fixture catches a candidate mismatch. Active scanning and the negative fixture share the violation collector; 9/9 source rows pass. The report now states the accepted radiation/diffraction/pressure/traction/A-B chain. Checkcode/SHA close 23 files; no fingerprinted solver dependency changed, so no physical rerun is needed. Freeze `78aff7cdc...`. Orthogonal design may resume, but formal sweep execution still requires its exact design/manifests to clear OD-01/OD-04 and related run-level gates. |
| 2026-08-30 | Checkpoint 2 — Core-A / orthogonal-manifest review | BLOCKER / P-FULL PREWORK AUTHORIZED | Production-path geometry preflight proves Core-A is a real held-resolution common-radius screen: 15/15 states pass directional tolerance, expected counts and component-hash changes. It does not establish adequate high-frequency resolution or convergence. Current fixed-56 azimuthal coupling leaves B/C incomplete and prevents pure top-only/bottom-only extent attribution; P-min alone is insufficient. P-full endpoint-independent azimuthal controls plus unequal-count wall-zipper CP4 evidence are required. Sponge-start factor 7 also needs a matched-width baseline. No formal BEM sweep is authorized; `78aff7cdc...` is only the pre-P-full fingerprint. |
| 2026-08-30 | Checkpoint 2 — P-full atomic implementation plan | PASS / PATCH ONLY | Executor may implement the one-issue mesh/control patch: common and endpoint azimuthal resolution, unequal-count watertight zipper, manifests/cache/fingerprint and focused execution gates. Factors 2/4 must still have only one independent radius input; endpoint counts are internally derived under a frozen spacing rule and the fixed horizontal mesh is bitwise invariant. Degenerate-quad triangles require positive/finite production quadrature and active-assembly evidence. The matched 0.75-lambda start/width bridge is accepted. No physics-formula change, official sweep or convergence claim is authorized; post-patch CP4 and a new fingerprint/clean baseline remain mandatory. |
| 2026-08-30 | Checkpoint 2 — P-full unequal-wall active finding | BLOCKER / GEOMETRY REDESIGN AUTHORIZED | A real unequal-count wall produces nonzero tangential normals (`max |n_theta|=0.0163591`) and is correctly rejected by the axisymmetric local-ABC helper. Dropping that component or its assertion would be an unreviewed physics change; general-normal ABC is not authorized. Executor may instead keep a common structured-wall count and place finite-width count-transition adapters on the horizontal FS/bottom surfaces. Fixed-side meshes/counts/hashes and `N_wall` must be bitwise invariant, the wall must retain the existing ABC/tolerance, adapter BC classification and quadrature/topology must pass, and a worst-radius resolution rule plus matched-count bridge is required. No assembly pilot or sweep precedes revised CP4 evidence. |
